# omd v0.1.0 (R1) — 위생 + 핵심 게이트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** omd의 실측 결함 7건(H1–H7)을 수리하고, 버전 SSOT·배포 불변식·핵심 코드 게이트 3종(G1/G4/G7)+채굴 3건(AC-1a/IA-1/HG-3)을 확립해 v0.1.0으로 릴리스한다.

**Architecture:** spec `docs/superpowers/specs/2026-07-11-omd-program-design.md` §5 R1 행 + R1 build-order constraints를 태스크로 분해. 순서: 테스트 green 복구(T1–T2) → 버전 SSOT(T3) → 위생(T4–T7) → 신규 회귀 테스트(T8) → 게이트 G7→모델정합→AC-1a→G4→G1→HG-3(T9–T14) → Release(T15). 모든 신규 훅은 stdlib-only·fail-open·advisory (D6: `decision:block` 금지).

**Tech Stack:** Python 3 stdlib only (훅·테스트), pytest, JSON 훅 계약 (Claude Code hooks).

## Global Constraints

- **작업 루트**: `/Users/kimseungmin/.claude/worktrees/oh-my-docs-r1` (브랜치 `feat/v0.1.0-r1`). 모든 경로는 이 루트 기준 상대경로.
- **테스트 명령**: `python3 -m pytest tests/ -q` — 각 태스크 종료 시점에 전체 suite green (단, T1 종료 시점에는 T2가 고칠 1건만 잔존 FAIL 허용).
- **훅 원칙 (D6)**: stdlib-only import, 모든 예외는 `return 0` (fail-open), 출력은 advisory(`additionalContext`/`systemMessage`)만 — `"decision": "block"` 절대 금지.
- **배포 불변식 (D8)**: skills/·agents/·hooks/·references/ 에 개인 홈경로·이메일·머신 특정 절대경로 금지. 테스트 코드의 금지어 목록에도 개인 식별자 리터럴을 새기지 말 것 (정규식 패턴만 — spec DT-3).
- **커밋 컨벤션**: 기존 이력 준수 — `feat(omd):` / `fix(omd):` / `test(omd):` / `docs(omd):`. 각 커밋 말미에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
- **기존 오피스 파이프라인 무변경**: 이 plan에 명시된 편집 외에 기존 4포맷 카드·verify 경로 동작 변경 금지 (하위 호환, spec §7).
- **prose 언어**: 스킬/에이전트/카드 신규 문장은 영어(588c460 영어화 이후 기조), `route_emit.py` CHECKPOINT와 훅 리마인더 본문은 한국어(기존 스타일 유지).

---

### Task 1: H1 — docs-pdf 스킬 plugin.json 등록

**Files:**
- Modify: `.claude-plugin/plugin.json` (skills 배열)
- Test: `tests/test_plugin_integrity.py` (기존 — 수정 없음)

**Interfaces:**
- Consumes: 없음
- Produces: skills 배열에 `"./skills/docs-pdf/"` 항목 (T15 Release가 이 파일의 version 필드를 bump)

- [ ] **Step 1: 실패 테스트 확인**

Run: `python3 -m pytest tests/test_plugin_integrity.py::test_every_skill_dir_is_registered -v`
Expected: FAIL — `skills present on disk but unregistered in plugin.json: ['docs-pdf']`

- [ ] **Step 2: plugin.json 수정**

`.claude-plugin/plugin.json`의 skills 배열 마지막 항목 뒤에 추가:

```json
    "./skills/docs-learn/",
    "./skills/docs-pdf/"
```

(현재 배열은 `"./skills/docs-learn/"`로 끝남 — 그 뒤에 `"./skills/docs-pdf/"` 추가, JSON 콤마 주의)

- [ ] **Step 3: 테스트 통과 확인**

Run: `python3 -m pytest tests/test_plugin_integrity.py -v`
Expected: 3 passed

- [ ] **Step 4: 전체 suite 확인**

Run: `python3 -m pytest tests/ -q`
Expected: `1 failed, 32 passed` — 잔존 1건은 T2가 고칠 `test_cross_project_confidentiality_gate_present`만

- [ ] **Step 5: Commit**

```bash
git add .claude-plugin/plugin.json
git commit -m "fix(omd): register docs-pdf skill in plugin.json (H1)"
```

---

### Task 2: H2 — docs-pilot '식별자 스크럽' 앵커 복원

**Files:**
- Modify: `skills/docs-pilot/SKILL.md:57`
- Test: `tests/test_wiki_two_level.py` (기존 — 수정 없음)

**Interfaces:**
- Consumes: 없음
- Produces: 없음

**배경**: 커밋 588c460(문서 영어화)이 한국어 "식별자 스크럽"을 "project-identifier scrub"으로 옮기며 테스트가 assert하는 한국어 리터럴이 소실됨. 기밀 격리 계약의 이중 언어 앵커를 복원한다 (테스트가 계약 — 테스트 수정 금지).

- [ ] **Step 1: 실패 테스트 확인**

Run: `python3 -m pytest tests/test_wiki_two_level.py::test_cross_project_confidentiality_gate_present -v`
Expected: FAIL — `assert '식별자 스크럽' in ...`

- [ ] **Step 2: docs-pilot 수정**

`skills/docs-pilot/SKILL.md` 57행에서 이 부분(old_string):

```
**content 0 (document text, figures, and claims are permanently forbidden globally) + project-identifier scrub** (remove customer names,
```

을 다음으로 교체(new_string):

```
**content 0 (document text, figures, and claims are permanently forbidden globally) + project-identifier scrub (식별자 스크럽)** (remove customer names,
```

- [ ] **Step 3: 테스트 통과 확인**

Run: `python3 -m pytest tests/ -q`
Expected: `33 passed` (전체 green 복구)

- [ ] **Step 4: Commit**

```bash
git add skills/docs-pilot/SKILL.md
git commit -m "fix(omd): restore 식별자 스크럽 bilingual anchor in docs-pilot (H2)"
```

---

### Task 3: H3 — 버전 SSOT 확립 + CHANGELOG 정책 전환

**Files:**
- Modify: `.claude-plugin/plugin.json` (version 필드 신설)
- Modify: `CHANGELOG.md` (헤더 정책 블록 교체)
- Create: `tests/test_version_sync.py`

**Interfaces:**
- Consumes: T1의 plugin.json
- Produces: `plugin.json["version"]` (SSOT — T15가 `0.1.0`으로 bump), `tests/test_version_sync.py::SEMVER_RE`

**결정 기록**: 현행 commit-SHA versioning(CHANGELOG.md:5 명시)을 keep-a-changelog 1.1.0 + semver로 전환. spec §3.3이 "CHANGELOG는 keep-a-changelog 수동 저작+기계 검증"으로 잠금했고 릴리스 트레인 자체가 semver 전제. **정책 변경이므로 PR 본문에 사용자 승인 요청 항목으로 명시할 것.** fan-out은 plugin.json 단독(omd에 pyproject 없음, README에 버전 마커 없음 — spec DT-1·DT-2, README 버전 배지 신설은 R2 dogfooding에서 별도 판단).

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_version_sync.py`:

```python
"""Version SSOT — plugin.json's version field is the single source of truth (R1/H3).

omx precedent (test_version_sync.py), reduced per spec DT-1/DT-2: omd is a pure
plugin (no pyproject.toml) and README carries no version marker, so the only
mechanical check is "the SSOT field exists and is valid semver". Fan-out targets
are added here the day they exist — never injected preemptively (omx policy).
"""
import json
import re
from pathlib import Path

ROOT = Path(__file__).parent.parent
PLUGIN_JSON = ROOT / ".claude-plugin" / "plugin.json"

SEMVER_RE = re.compile(r"^\d+\.\d+\.\d+$")


def test_version_field_exists():
    plugin = json.loads(PLUGIN_JSON.read_text(encoding="utf-8"))
    assert "version" in plugin, "plugin.json must carry the version SSOT field (H3)"


def test_version_is_valid_semver():
    plugin = json.loads(PLUGIN_JSON.read_text(encoding="utf-8"))
    assert SEMVER_RE.match(plugin.get("version", "")), (
        f"version must be MAJOR.MINOR.PATCH semver, got: {plugin.get('version')!r}"
    )
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_version_sync.py -v`
Expected: 2 FAIL (`version` 키 부재)

- [ ] **Step 3: plugin.json에 version 신설**

`.claude-plugin/plugin.json`에서:

```json
  "name": "oh-my-docs",
```

을 다음으로 교체:

```json
  "name": "oh-my-docs",
  "version": "0.0.0",
```

(`0.0.0` = pre-release 표식. T15 Release가 `0.1.0`으로 bump — 버전 bump는 항상 Release 태스크의 소관)

- [ ] **Step 4: CHANGELOG 정책 블록 교체**

`CHANGELOG.md`에서 이 블록(old — 3~8행):

```
> **Versioning policy**: omd deliberately uses **commit-SHA versioning** (no individual release numbers).
> This file tracks only the *contract change history* — when a surface that other
> components (omha, the session LLM) depend on changes, such as a hook's emit format, it is recorded here. General content
> changes have the git log as the SSOT. Tone unified with the OMS (oh-my-scholar) CHANGELOG.
```

을 다음으로 교체(new):

```
The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/). Version
SSOT: `.claude-plugin/plugin.json` `version`.

> **Policy note (2026-07-13, R1)**: the earlier deliberate **commit-SHA versioning** policy is
> superseded by the R1–R4 release train (spec: `docs/superpowers/specs/2026-07-11-omd-program-design.md` §5).
> Entries below predating v0.1.0 were written under the old policy and stay as-is;
> the git log remains the SSOT for pre-semver general content changes.
```

(`## [Unreleased]` 이하 기존 내용은 무변경 — T15가 0.1.0으로 이동)

- [ ] **Step 5: 테스트 통과 + 전체 suite**

Run: `python3 -m pytest tests/ -q`
Expected: `35 passed`

- [ ] **Step 6: Commit**

```bash
git add .claude-plugin/plugin.json CHANGELOG.md tests/test_version_sync.py
git commit -m "feat(omd): establish version SSOT + switch CHANGELOG to keep-a-changelog/semver (H3)"
```

---

### Task 4: H4 — docs_verify_emit 위생 (죽은 DOC_EXTS 제거 + xlsx 신호 추가)

**Files:**
- Modify: `hooks/docs_verify_emit.py`
- Test: `tests/test_verify_emit.py` (케이스 추가)

**Interfaces:**
- Consumes: 없음
- Produces: `is_doc_build()` 시그니처 불변 — T13·T14가 같은 파일을 이어서 수정 (이 태스크 먼저)

- [ ] **Step 1: 기존 테스트 파일 정독**

`tests/test_verify_emit.py`를 읽고 기존 9케이스의 훅 호출 패턴(모듈 import 방식인지 subprocess stdin 방식인지)을 파악, 신규 케이스는 같은 패턴으로 작성.

- [ ] **Step 2: 실패 테스트 추가**

`tests/test_verify_emit.py`에 3케이스 추가. ⚠️ 기존 9케이스는 subprocess 패턴이지만 아래 3케이스는 **모듈 import 패턴**을 쓴다 — 파일 상단에 `from hooks.docs_verify_emit import is_doc_build`와 `import hooks.docs_verify_emit as mod`를 추가할 것 (모듈 속성 introspection은 subprocess로 불가능하며, `python3 -m pytest tests/`가 repo 루트를 sys.path에 올려 namespace 패키지로 해석됨 — critic 실측 확인):

```python
def test_xlsx_engine_signal_triggers():
    # openpyxl 기반 xlsx 생성 명령이 리마인더를 발화해야 한다 (H4: xlsx 누락 수리)
    assert is_doc_build("python3 -c 'from openpyxl import Workbook; ...'")


def test_xlsx_named_script_triggers():
    # 빌더 권장 경로의 xlsx 스크립트 실행도 발화해야 한다
    assert is_doc_build("python3 build_xlsx_report.py")


def test_dead_doc_exts_removed():
    # H4: 죽은 상수가 다시 생기지 않도록 — 모듈에 DOC_EXTS가 없어야 한다
    import hooks.docs_verify_emit as mod
    assert not hasattr(mod, "DOC_EXTS")
```

Run: `python3 -m pytest tests/test_verify_emit.py -v`
Expected: 신규 3건 FAIL, 기존 9건 PASS

- [ ] **Step 3: 훅 수정**

`hooks/docs_verify_emit.py`:

1. 25행 `DOC_EXTS = (".pptx", ".docx", ".hwpx")` 줄과 그 위 주석 삭제 (죽은 상수).
2. `BUILD_SIGNALS` 튜플에 xlsx 엔진 신호 3개 추가:

```python
BUILD_SIGNALS = (
    "python-pptx", "python-docx", "from pptx", "import pptx",
    "from docx", "import docx", "Presentation(", "Document(",
    "openpyxl", "xlsxwriter", "Workbook(",
    "soffice --convert", "libreoffice --convert", "--convert-to",
)
```

3. `RUN_SCRIPT_RE`의 대체군에 `xlsx` 추가:

```python
RUN_SCRIPT_RE = re.compile(
    r"\bpython3?\b[^\n|&;]*\b\w*(build|deck|slide|doc|pptx|docx|xlsx|hwpx|present)\w*\.py\b",
    re.IGNORECASE,
)
```

4. 모듈 docstring 1~2행의 `(.pptx/.docx/.hwpx via python-pptx / python-docx / soffice)`를 `(.pptx/.docx/.xlsx/.hwpx via python-pptx / python-docx / openpyxl / soffice)`로 갱신.
5. `build_reminder()` 마지막 항목의 `pptx/docx/hwpx 포맷 원형 유지`를 `pptx/docx/xlsx/hwpx 포맷 원형 유지`로 갱신.

- [ ] **Step 4: 테스트 통과 + 전체 suite**

Run: `python3 -m pytest tests/ -q`
Expected: `38 passed`

- [ ] **Step 5: Commit**

```bash
git add hooks/docs_verify_emit.py tests/test_verify_emit.py
git commit -m "fix(omd): drop dead DOC_EXTS, add xlsx build signals to verify-emit hook (H4)"
```

---

### Task 5: H5 — pdf 층위 표기 (route_emit CHECKPOINT + 회귀 테스트)

**Files:**
- Modify: `hooks/route_emit.py` (CHECKPOINT 본문 1줄 추가)
- Test: `tests/test_route_emit.py` (케이스 추가)

**Interfaces:**
- Consumes: 없음
- Produces: CHECKPOINT에 "입력·변환 층위" 앵커 문자열 (테스트가 assert)

**결정 기록 (H5)**: pdf는 FORMAT 슬롯에 편입하지 않는다 — 생성 포맷이 아니라 입력(docs-pdf)·변환 타깃(docs-convert) 층위이므로 별도 표기. 이 결정을 CHECKPOINT 본문+회귀 테스트로 기계화해 이후 "pdf도 포맷에 추가하자" 회귀를 차단.

- [ ] **Step 1: 실패 테스트 추가**

`tests/test_route_emit.py`에 기존 호출 헬퍼(예: `test_context_lists_formats`가 쓰는 것)를 재사용해 추가:

```python
def test_pdf_is_not_a_generation_format():
    """H5 결정: pdf는 입력·변환 층위 — FORMAT/STAGE 슬롯에 절대 편입 금지."""
    out = <기존 헬퍼로 CHECKPOINT 텍스트 획득>
    stage_lines = [l for l in out.splitlines() if l.startswith("STAGE(docs)")]
    assert stage_lines, "STAGE token line must exist"
    assert "pdf" not in stage_lines[0]          # STAGE 토큰 줄에 pdf 없음
    assert "입력·변환 층위" in out                 # 층위 표기는 존재
```

Run: `python3 -m pytest tests/test_route_emit.py::test_pdf_is_not_a_generation_format -v`
Expected: FAIL (`입력·변환 층위` 부재)

- [ ] **Step 2: CHECKPOINT 수정**

`hooks/route_emit.py`의 CHECKPOINT에서 이 줄(old, :24):

```python
    "- 포맷: pptx / docx / xlsx / hwpx — references/formats/<format>.md 카드가 도구·함정·수식의 단일 진실.\n"
```

바로 다음에 새 줄 추가(new):

```python
    "- 포맷: pptx / docx / xlsx / hwpx — references/formats/<format>.md 카드가 도구·함정·수식의 단일 진실.\n"
    "  (pdf 는 생성 포맷이 아니라 입력·변환 층위 — 입력=docs-pdf, 변환 타깃=docs-convert. FORMAT 판정에 pdf 를 넣지 말 것.)\n"
```

- [ ] **Step 3: 테스트 통과 + 전체 suite**

Run: `python3 -m pytest tests/ -q`
Expected: `39 passed` (기존 route_emit 13케이스 전부 유지 — CHECKPOINT 텍스트 회귀 확인 포함)

- [ ] **Step 4: Commit**

```bash
git add hooks/route_emit.py tests/test_route_emit.py
git commit -m "feat(omd): pin pdf as input/convert layer in routing checkpoint (H5)"
```

---

### Task 6: H7 — themes 프리셋을 docs-build·docs-standardize에 연결

**Files:**
- Modify: `skills/docs-build/SKILL.md` (`<Format_Knowledge>` 블록)
- Modify: `skills/docs-standardize/SKILL.md` (`<Do_Not_Use_When>` 블록)

**Interfaces:**
- Consumes: 없음
- Produces: 없음 (prose 연결 — 기계 테스트는 R2 skill-contract 테스트가 담당)

- [ ] **Step 1: docs-standardize 수정**

`skills/docs-standardize/SKILL.md`의 이 줄(old):

```
- When there is **no** existing document to reference at all → skip (build uses the default tone preset)
```

을 다음으로 교체(new):

```
- When there is **no** existing document to reference at all → skip (build uses the default tone preset — the concrete fallback is `references/themes/` (10 font/color presets); offer those instead of guessing)
```

- [ ] **Step 2: docs-build 수정**

`skills/docs-build/SKILL.md`의 `<Format_Knowledge>` 블록에서 hwpx 행 다음에 추가:

```
- no-reference fallback → references/themes/ (10 font/color presets — only when standardize was skipped for lack of reference documents; an induced style spec always wins over a preset)
```

- [ ] **Step 3: 연결 검증**

Run: `grep -l "references/themes" skills/docs-build/SKILL.md skills/docs-standardize/SKILL.md`
Expected: 두 파일 모두 출력. 이어서 `python3 -m pytest tests/ -q` → `39 passed` (회귀 없음)

- [ ] **Step 4: Commit**

```bash
git add skills/docs-build/SKILL.md skills/docs-standardize/SKILL.md
git commit -m "fix(omd): wire references/themes fallback into build/standardize skills (H7)"
```

---

### Task 7: H6 — README Status·Structure 최신화

**Files:**
- Modify: `README.md` (Structure 블록 :14-22, Status 절 :32-40)

**Interfaces:**
- Consumes: 없음
- Produces: 없음 (T15 Release가 최종 정합 재확인)

- [ ] **Step 1: Structure 블록 갱신**

`README.md`의 Structure 코드블록(`skills/ ...`부터 `hooks/docs_verify_emit.py ...`까지 — ⚠️ 줄번호는 신뢰하지 말고 파일을 읽어 현재 텍스트에 앵커; 현재 skills 줄은 7개 스킬만 나열된 stale 상태)을 다음으로 교체:

```
skills/   docs-intake · docs-standardize · docs-plan · docs-build · docs-inspect · docs-verify
          docs-convert · docs-revise · docs-translate · docs-pilot · docs-learn · docs-pdf
agents/   doc-analyzer · doc-planner · doc-builder · doc-inspector · doc-verifier   (OMC XML 7-section)
references/formats/{pptx,docx,xlsx,hwpx}.md   format tools/traps/formula knowledge (single source of truth)
references/formats/pdf.md                     pdf as *input/convert layer* (not a generation format)
references/rubrics/ppteval.md                 Content/Design/Coherence (inspect + verify)
references/themes/                            10 fallback font/color presets (no-reference branch)
references/wiki/README.md                     two-level wiki contract (local + global ascent)
hooks/route_emit.py                           UserPromptSubmit format/stage routing checkpoint (stdlib only)
hooks/docs_verify_emit.py                     PostToolUse — after a Bash document build/convert, a
                                              freeze-safe docs-verify reminder (stdlib only, fail-open)
```

- [ ] **Step 2: Status 절 갱신**

`## Status (2026-05-31)` 절 전체(제목 포함, "Design: ..." 줄 전까지 — 실제 위치는 파일을 읽어 확인, 현재 :31 부근)를 다음으로 교체:

```
## Status (2026-07-13)

**v0.1.0 (R1 — hygiene + core gates).** All four office format cards are complete
(**pptx, docx, xlsx, hwpx**), plus a pdf *input-side* card (`docs-pdf` skill) and a two-level
wiki (local + global ascent). 12 skills · 5 agents · hooks with regression tests
(`python3 -m pytest tests/`). Formula rendering proven by real soffice renders: **docx** native
OMML path VERIFIED (editable math, with two documented soffice caveats — `\hat` accent,
`\sum`/`\,` tofu box) plus matplotlib-image fallback; **pptx** is matplotlib-image only
(soffice/Impress renders native OMML blank). The pptx path is validated end-to-end in live
sessions; docx/xlsx/hwpx build paths follow their cards.

Roadmap: R1 hygiene+gates (this release) → R2 repo-docs genre → R3 MkDocs site genre →
R4 knowledge lifecycle — see `docs/superpowers/specs/2026-07-11-omd-program-design.md`.
```

- [ ] **Step 3: 회귀 확인 + Commit**

Run: `python3 -m pytest tests/ -q` → `39 passed`

```bash
git add README.md
git commit -m "docs(omd): refresh README status/structure to current inventory (H6)"
```

---

### Task 8: 신규 회귀 테스트 2종 — distribution-axiom + skill 예산 (D8·IA-1, 동일 커밋)

**Files:**
- Create: `tests/test_distribution_axiom.py`
- Create: `tests/test_skill_budget.py`

**Interfaces:**
- Consumes: 없음
- Produces: `SCAN_ROOTS` 관례 (배포 트리 = skills/, agents/, hooks/, references/, .claude-plugin/)

- [ ] **Step 1: distribution-axiom 테스트 작성**

Create `tests/test_distribution_axiom.py`:

```python
"""Distribution axiom (D8) — omd is a distributed plugin: no personal identifiers,
no absolute home paths, no machine-specific values in any shipped file.

Per spec DT-3: patterns only, NEVER literal personal tokens (writing this session's
username into the forbidden list would itself violate the axiom). Placeholders like
`/Users/<you>` are allowed (the `<` immediately after the home root marks a template).
"""
import re
from pathlib import Path

ROOT = Path(__file__).parent.parent
SCAN_ROOTS = ["skills", "agents", "hooks", "references", ".claude-plugin"]
SCAN_SUFFIXES = {".md", ".py", ".json", ".txt"}

HOME_PATH_RE = re.compile(r"/(?:Users|home)/(?!<)[A-Za-z0-9_.-]+")
EMAIL_RE = re.compile(r"[A-Za-z0-9_.+-]+@[A-Za-z0-9-]+\.[A-Za-z0-9.-]+")


def shipped_files():
    for root in SCAN_ROOTS:
        for p in (ROOT / root).rglob("*"):
            if p.is_file() and p.suffix in SCAN_SUFFIXES:
                yield p


def _hits(pattern):
    out = []
    for p in shipped_files():
        for i, line in enumerate(p.read_text(encoding="utf-8", errors="replace").splitlines(), 1):
            if pattern.search(line):
                out.append(f"{p.relative_to(ROOT)}:{i}: {line.strip()[:100]}")
    return out


def test_no_absolute_home_paths():
    hits = _hits(HOME_PATH_RE)
    assert not hits, "absolute home paths in shipped files:\n" + "\n".join(hits)


def test_no_email_addresses():
    hits = _hits(EMAIL_RE)
    assert not hits, "email addresses in shipped files:\n" + "\n".join(hits)
```

- [ ] **Step 2: skill 예산 테스트 작성**

Create `tests/test_skill_budget.py`:

```python
"""Skill context budget (IA-1) — the sum of all SKILL.md bodies is capped at 100 KiB.

Independently derived from omd's own measured footprint (~58 KiB on 2026-07-11), NOT
from OMC's 64 KiB figure (that is a post-compression yardstick — do not analogize).
Lock the cap BEFORE R2 grows the bodies (same logic as G7: rule before the fleet doubles).
"""
from pathlib import Path

ROOT = Path(__file__).parent.parent
BUDGET_BYTES = 100 * 1024


def test_skill_bodies_within_budget():
    sizes = {p: p.stat().st_size for p in (ROOT / "skills").glob("*/SKILL.md")}
    total = sum(sizes.values())
    assert total <= BUDGET_BYTES, (
        f"skills/*/SKILL.md total {total} B exceeds {BUDGET_BYTES} B budget; "
        "largest: " + ", ".join(f"{p.parent.name}={s}" for p, s in sorted(sizes.items(), key=lambda kv: -kv[1])[:3])
    )
```

- [ ] **Step 3: 실행 — green 출발 확인**

Run: `python3 -m pytest tests/test_distribution_axiom.py tests/test_skill_budget.py -v`
Expected: 3 passed. **만약 distribution 테스트가 실제 개인 식별자를 잡으면**: 해당 배포 파일을 수리(플레이스홀더화)한다 — 테스트를 완화하지 말 것. 플레이스홀더 오탐(`/Users/<you>` 류)이면 정규식만 정밀화.

- [ ] **Step 4: 전체 suite + Commit**

Run: `python3 -m pytest tests/ -q` → `42 passed`

```bash
git add tests/test_distribution_axiom.py tests/test_skill_budget.py
git commit -m "test(omd): distribution-axiom + skill budget regression guards (D8, IA-1)"
```

---

### Task 9: G7 — 엔진 버전 고정 규칙 (카드 계약 + drift 강등)

**Files:**
- Create: `references/formats/README.md`
- Modify: `agents/doc-builder.md` (`<Investigation_Protocol>`에 1스텝)
- Modify: `agents/doc-verifier.md` (`<Investigation_Protocol>`에 1스텝)
- Create: `tests/test_format_cards.py`

**Interfaces:**
- Consumes: 없음
- Produces: 카드 계약 문서(신규 카드 작성 규칙 — R2·R3의 repo-docs/site 카드가 이 계약을 따름), `UNVERIFIED (engine drift)` verdict 어휘

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_format_cards.py`:

```python
"""Format card contract (G7) — every generation card pins its engine versions, and
the card-authoring contract (references/formats/README.md) defines the engine-drift
demotion rule. Locked BEFORE R2/R3 double the card count (retrofit is expensive).
"""
import re
from pathlib import Path

ROOT = Path(__file__).parent.parent
FORMATS_DIR = ROOT / "references" / "formats"
GENERATION_CARDS = ["pptx.md", "docx.md", "xlsx.md", "hwpx.md"]  # pdf.md is input-side (H5)
VERSION_RE = re.compile(r"\d+\.\d+")


def _engine_section(text: str) -> str:
    m = re.search(r"^## Engine\b(.*?)(?=^## |\Z)", text, re.MULTILINE | re.DOTALL)
    assert m, "card must carry an '## Engine' section"
    return m.group(1)


def test_generation_cards_pin_engine_versions():
    for name in GENERATION_CARDS:
        text = (FORMATS_DIR / name).read_text(encoding="utf-8")
        engine = _engine_section(text)
        assert VERSION_RE.search(engine), f"{name}: Engine section pins no version number"


def test_card_contract_defines_engine_drift_rule():
    contract = (FORMATS_DIR / "README.md").read_text(encoding="utf-8")
    assert "UNVERIFIED (engine drift)" in contract
    assert "## Engine" in contract  # 필수 섹션 계약 명시
```

Run: `python3 -m pytest tests/test_format_cards.py -v`
Expected: `test_card_contract_defines_engine_drift_rule` FAIL (README 부재). `test_generation_cards_pin_engine_versions`는 통과 예상(pptx 카드 Engine 표가 이미 버전 고정) — **hwpx/xlsx/docx 카드에서 FAIL하면 해당 카드 Engine 표에 실측 버전을 채워 통과시킨다 (이 머신에서 `python3 -c "import <engine>; print(<engine>.__version__)"` 실측 후 기입).**

- [ ] **Step 2: 카드 계약 문서 작성**

Create `references/formats/README.md`:

```markdown
# Format Cards — authoring contract

> Data files, not skills. Every card is the single source of truth for one format's
> tools, traps, and verified techniques. Agents (`doc-builder`, `doc-verifier`,
> `doc-inspector`) read the card; they never duplicate its knowledge.

## Required sections (every card)

The three sections all five existing cards share — new cards (repo-docs, site, …)
must carry them too; everything else is format-specific and free-form:

1. **`## Engine`** — the borrowed tools, one table row per tool, each row pinning the
   **measured version** (e.g. `python-pptx` 1.0.2) and its verification status on this
   machine. A row without a version number is not a pin.
2. **Hard traps** — API/format traps with `[VERIFIED ✓ — date]`-stamped evidence.
3. **Version-snapshot policy** — how `.omd/<slug>/versions/` snapshots work for this format.

Conditional: a `License_Note` section whenever material is borrowed from an external
source (anthropics/skills convention).

## VERIFIED stamp grammar

New stamps carry the engine and version whenever the claim is engine-dependent:

    [VERIFIED ✓ — YYYY-MM-DD, <engine> <version>]     e.g. [VERIFIED ✓ — 2026-06-16, python-pptx 1.0.2]

Legacy stamps (date-only, or `this machine`) are grandfathered — normalize them only
when the surrounding section is next re-verified (R5+ sweep; do not bulk-rewrite).

## Engine-drift demotion rule (G7)

At build/verify time the agent measures the live engine version (e.g.
`python3 -c "import pptx; print(pptx.__version__)"`; `soffice --version`) and compares it
against the card's `## Engine` pins. On mismatch, every VERIFIED claim tied to that
engine is treated as **`UNVERIFIED (engine drift)`** for this run — state the demotion
in the report (builder: Build Notes; verifier: Verification Report), then either
re-verify the claim live or proceed with the honest UNVERIFIED label. Never silently
trust a stamp measured on a different engine version. (Same family as the D3 rule:
a missing engine yields `UNVERIFIED (engine unavailable)`, never a silent PASS.)
```

- [ ] **Step 3: 에이전트 규칙 1줄씩 추가**

`agents/doc-builder.md`의 `<Investigation_Protocol>` 목록 끝에 추가 (⚠️ 기존 목록이 `1)`–`n)` 번호 형식이면 `-` 불릿 대신 다음 번호 `n+1)`로 맞춰 붙일 것):

```
    - Engine-version pin check (G7): before building, measure the live engine version and compare with the card's `## Engine` pins (contract: references/formats/README.md). On mismatch, flag `UNVERIFIED (engine drift)` in Build Notes — the card's VERIFIED stamps for that engine are not trusted this run.
```

`agents/doc-verifier.md`의 `<Investigation_Protocol>` 목록 끝에 추가 (동일 — 번호 형식에 맞출 것):

```
    - Engine-version pin check (G7): measure the live engine version and compare with the card's `## Engine` pins (contract: references/formats/README.md). On mismatch, record `UNVERIFIED (engine drift)` in the Verification Report and re-verify affected claims fresh instead of trusting the card's stamps.
```

- [ ] **Step 4: 테스트 통과 + 전체 suite**

Run: `python3 -m pytest tests/ -q` → `44 passed`

- [ ] **Step 5: Commit**

```bash
git add references/formats/README.md agents/doc-builder.md agents/doc-verifier.md tests/test_format_cards.py
git commit -m "feat(omd): engine-version pin contract + drift demotion rule (G7)"
```

---

### Task 10: 에이전트 model 정합 — doc-planner opus→sonnet + model 정책 테스트

**Files:**
- Modify: `agents/doc-planner.md:4` (frontmatter)
- Modify: `skills/docs-plan/SKILL.md` (`--consensus` path 2c)
- Create: `tests/test_agent_contract.py` (model 정책 부분)

**Interfaces:**
- Consumes: 없음
- Produces: `tests/test_agent_contract.py`의 `_frontmatter()` 헬퍼와 `EXPECTED_MODELS` (T11이 같은 파일에 마커 테스트 추가), `EXPECTED_MODELS` 자체가 T12 model_guard의 진실 기준

**배경 (완전성 비평 #2)**: doc-planner `model: opus` 고정은 기본 `--direct` 경로까지 opus로 태워 G4와 자기모순. sonnet으로 내리고 Deliberate(`--consensus`)만 호출 시점 opus 승격.

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_agent_contract.py`:

```python
"""Agent contract guards (R1): frontmatter model policy (G4 precondition) and, from
Task 11, the Final_Response_Contract markers (AC-1a). stdlib frontmatter parse only.
"""
from pathlib import Path

ROOT = Path(__file__).parent.parent
AGENTS_DIR = ROOT / "agents"

# Deliberate judgment lanes (inspector/verifier) run opus; production lanes run sonnet.
# doc-planner is sonnet by default — Deliberate consensus escalates at Task-call time
# (skills/docs-plan SKILL.md --consensus path), never via frontmatter.
EXPECTED_MODELS = {
    "doc-analyzer": "sonnet",
    "doc-builder": "sonnet",
    "doc-planner": "sonnet",
    "doc-inspector": "opus",
    "doc-verifier": "opus",
}


def _frontmatter(name: str) -> dict:
    lines = (AGENTS_DIR / f"{name}.md").read_text(encoding="utf-8").splitlines()
    assert lines[0] == "---", f"{name}: frontmatter must open the file"
    fm = {}
    for line in lines[1:]:
        if line == "---":
            return fm
        if ":" in line:
            k, _, v = line.partition(":")
            fm[k.strip()] = v.strip()
    raise AssertionError(f"{name}: unterminated frontmatter")


def test_agent_model_policy():
    for name, model in EXPECTED_MODELS.items():
        assert _frontmatter(name).get("model") == model, (
            f"{name}: frontmatter model must be {model} (spec §3.1 G4 / critique #2)"
        )
```

Run: `python3 -m pytest tests/test_agent_contract.py -v`
Expected: FAIL — `doc-planner: frontmatter model must be sonnet`

- [ ] **Step 2: doc-planner frontmatter 수정**

`agents/doc-planner.md` 4행 `model: opus` → `model: sonnet`

- [ ] **Step 3: docs-plan consensus 승격 문구 추가**

`skills/docs-plan/SKILL.md`의 `--consensus` path 2c(old):

```
2c. **planner** dispatch (`--consensus` directive): triggers `<Consensus_RALPLAN_DR_Protocol>` — Principles + Drivers + arc Options≥2 + steelman + tradeoff + ADR + (if Deliberate) pre-mortem. Output = `plan.md` + outline.
```

을 다음으로 교체(new — 문장 하나 추가):

```
2c. **planner** dispatch (`--consensus` directive): triggers `<Consensus_RALPLAN_DR_Protocol>` — Principles + Drivers + arc Options≥2 + steelman + tradeoff + ADR + (if Deliberate) pre-mortem. Output = `plan.md` + outline. Dispatch this Task with `model: opus` — Deliberate consensus is the one planner path that warrants opus; the default `--direct` path runs at the agent's frontmatter sonnet.
```

- [ ] **Step 4: 테스트 통과 + 전체 suite**

Run: `python3 -m pytest tests/ -q` → `45 passed`

- [ ] **Step 5: Commit**

```bash
git add agents/doc-planner.md skills/docs-plan/SKILL.md tests/test_agent_contract.py
git commit -m "feat(omd): doc-planner sonnet default + consensus-time opus escalation (G4 precondition)"
```

---

### Task 11: AC-1a — Final_Response_Contract 정적 강제 테스트

**Files:**
- Modify: `tests/test_agent_contract.py` (마커 테스트 추가)

**Interfaces:**
- Consumes: T10의 `_frontmatter()`·파일 구조
- Produces: 없음

- [ ] **Step 1: 실패-불가 확인 후 테스트 추가**

이 태스크는 현재 실물 마커를 계약으로 고정하는 회귀 가드다(마커는 이미 존재 → 테스트는 처음부터 green이어야 정상). `tests/test_agent_contract.py`에 추가:

```python
# Final_Response_Contract (AC-1a): the markers each agent's Output_Format promises.
# These are the strings downstream consumers (skills, humans) grep for — losing one
# in a doc rewrite is a silent contract break (exactly how H2 happened).
REQUIRED_MARKERS = {
    "doc-verifier": ["### Verdict", "**Status**: PASS | FAIL"],
    "doc-inspector": ["## Formative Review"],
    "doc-planner": ["## Narrative Arc", "## Outline", "## Coverage Check"],
    "doc-analyzer": ["## Source Inventory", "## Reference Design System"],
    "doc-builder": ["## Artifact", "## Build Notes"],
}

# Self-approval ban is load-bearing prose for the two review lanes only (spec §3.4 AC-1a).
SELF_APPROVAL_BANS = {
    "doc-verifier": "never self-approve",
    "doc-inspector": "never approve work",
}


def _body(name: str) -> str:
    return (AGENTS_DIR / f"{name}.md").read_text(encoding="utf-8")


def test_final_response_contract_markers():
    for name, markers in REQUIRED_MARKERS.items():
        body = _body(name)
        for marker in markers:
            assert marker in body, f"{name}: required output marker {marker!r} missing"


def test_self_approval_ban_present():
    for name, phrase in SELF_APPROVAL_BANS.items():
        assert phrase in _body(name), f"{name}: self-approval ban phrase {phrase!r} missing"
```

- [ ] **Step 2: green 확인 (마커 실물 대조)**

Run: `python3 -m pytest tests/test_agent_contract.py -v`
Expected: 전부 PASS. FAIL하면 **테스트의 마커 문자열을 실물에 맞춰 정정**(에이전트 파일을 고치는 게 아님 — 이 태스크는 현재 계약의 스냅샷 고정).

- [ ] **Step 3: 전체 suite + Commit**

Run: `python3 -m pytest tests/ -q` → `47 passed`

```bash
git add tests/test_agent_contract.py
git commit -m "test(omd): pin agent Final_Response_Contract markers + self-approval bans (AC-1a)"
```

---

### Task 12: G4 — model-guard PreToolUse(Task) 훅

**Files:**
- Create: `hooks/model_guard.py`
- Modify: `.claude-plugin/plugin.json` (PreToolUse Task 항목 추가)
- Create: `tests/test_model_guard.py`

**Interfaces:**
- Consumes: `agents/*.md` frontmatter (T10이 확정한 model 값들)
- Produces: 없음

**설계 (spec AC-2 정밀화)**: model 미지정 Task 호출이 기본 상태(정상) — 감지 대상은 ① 명시적으로 frontmatter와 다른 model을 지정한 호출 ② 존재하지 않는 `oh-my-docs:*` subagent_type(오타). 둘 다 advisory 경고만.

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_verify_emit.py`의 훅 호출 패턴을 먼저 읽고 동일 패턴으로 작성. Create `tests/test_model_guard.py` (핵심 케이스 — 호출 헬퍼는 기존 패턴 재사용):

```python
def _payload(subagent_type=None, model=None):
    ti = {}
    if subagent_type is not None:
        ti["subagent_type"] = subagent_type
    if model is not None:
        ti["model"] = model
    return {"tool_name": "Task", "tool_input": ti}

# 1) 명시 model이 frontmatter와 불일치 → advisory 발화 (경고 문구에 두 model 명시)
#    _payload("oh-my-docs:doc-verifier", "haiku") → additionalContext에 "opus"·"haiku" 포함
# 2) 명시 model이 frontmatter와 일치 → 침묵 (출력 없음)
#    _payload("oh-my-docs:doc-verifier", "opus")
# 3) model 미지정 → 침묵 (미지정이 기본 상태 — spec AC-2)
#    _payload("oh-my-docs:doc-builder")
# 4) 존재하지 않는 oh-my-docs 에이전트 → advisory (오타 감지)
#    _payload("oh-my-docs:doc-bulder", None) → additionalContext에 "unknown" 류 경고
# 5) 타 플러그인/일반 subagent → 침묵
#    _payload("general-purpose", "haiku")
# 6) 깨진 stdin(비JSON) → exit 0, 출력 없음 (fail-open)
```

Run: `python3 -m pytest tests/test_model_guard.py -v`
Expected: 전부 FAIL (훅 부재)

- [ ] **Step 2: 훅 구현**

구현 전 PreToolUse 출력 필드명(`hookSpecificOutput.additionalContext`)을 Claude Code hooks 공식 문서와 대조 확인 — 기존 omd 훅 2개가 같은 필드를 쓰므로 내부 일관성은 확보돼 있고, fail-open이라 오기재는 조용히 무시되지만 확인이 싸다. Create `hooks/model_guard.py`:

```python
"""OMD PreToolUse(Task) hook: advisory model-tier guard (G4). stdlib only, fail-open.

doc-verifier's trust model presumes "the verifier ran on opus" — but nothing enforced
it. This hook does NOT block (D6): it only warns when a Task call explicitly pins a
model that contradicts the agent's frontmatter, or names a nonexistent oh-my-docs
agent. An unspecified model is the NORMAL state (Claude Code applies the agent
definition's model) — never warn about absence (spec AC-2).
"""
import json
import sys
from pathlib import Path

AGENTS_DIR = Path(__file__).resolve().parent.parent / "agents"
PREFIX = "oh-my-docs:"


def frontmatter_model(agent_name: str):
    path = AGENTS_DIR / f"{agent_name}.md"
    if not path.is_file():
        return None  # unknown agent
    fm_open = False
    for line in path.read_text(encoding="utf-8", errors="replace").splitlines():
        if line.strip() == "---":
            if fm_open:
                break
            fm_open = True
            continue
        if fm_open and line.startswith("model:"):
            return line.partition(":")[2].strip()
    return ""  # agent exists, no model pin


def check(payload: dict):
    """Return an advisory message or None (silent)."""
    if payload.get("tool_name") != "Task":
        return None
    ti = payload.get("tool_input", {}) or {}
    sub = ti.get("subagent_type", "")
    if not isinstance(sub, str) or not sub.startswith(PREFIX):
        return None
    agent = sub[len(PREFIX):]
    pinned = frontmatter_model(agent)
    if pinned is None:
        known = ", ".join(sorted(p.stem for p in AGENTS_DIR.glob("doc-*.md")))
        return (f"[OMD model-guard] unknown agent '{sub}' — no agents/{agent}.md. "
                f"Known: {known}. (advisory — proceeding)")
    requested = ti.get("model")
    if requested and pinned and requested != pinned:
        return (f"[OMD model-guard] '{agent}' is pinned to model '{pinned}' in its frontmatter "
                f"but this call requests '{requested}'. If this is not a deliberate escalation "
                f"(e.g. docs-plan --consensus), drop the model override. (advisory — proceeding)")
    return None


def main() -> int:
    try:
        payload = json.load(sys.stdin)
        msg = check(payload)
        if msg:
            print(json.dumps({"hookSpecificOutput": {
                "hookEventName": "PreToolUse",
                "additionalContext": msg,
            }}))
    except Exception:
        pass  # fail-open — never block the session
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 3: plugin.json 등록**

`.claude-plugin/plugin.json`의 hooks 객체에 PreToolUse 키 추가 (기존 PostToolUse 항목과 동형):

```json
    "PreToolUse": [
      {
        "matcher": "Task",
        "hooks": [
          {
            "type": "command",
            "command": "python3",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/model_guard.py"]
          }
        ]
      }
    ],
```

- [ ] **Step 4: 테스트 통과 + 전체 suite**

Run: `python3 -m pytest tests/ -q`
Expected: `53 passed` (신규 6케이스 기준 — 실제 케이스 수에 맞춰 확인)

- [ ] **Step 5: Commit**

```bash
git add hooks/model_guard.py .claude-plugin/plugin.json tests/test_model_guard.py
git commit -m "feat(omd): advisory model-tier guard on Task calls (G4)"
```

---

### Task 13: G1 — verify-pending 센티널 + Stop-guard 핸드셰이크

**Files:**
- Modify: `hooks/docs_verify_emit.py` (arm/clear 로직)
- Create: `hooks/docs_stop_guard.py`
- Modify: `.claude-plugin/plugin.json` (Stop 항목 추가)
- Create: `tests/test_stop_guard.py`
- Test: `tests/test_verify_emit.py` (arm/clear 케이스 추가)

**Interfaces:**
- Consumes: T4가 정리한 `docs_verify_emit.py` (T4 이후 실행 필수)
- Produces: 센티널 파일 계약 — `.omd/<slug>/.verify-pending` 또는 slug 불명 시 `.omd/.verify-pending`, 내용 `{"armed_at": <epoch>, "command_head": <str>}`. T14가 같은 파일을 이어서 수정.

**설계 (spec G1 + HK-1~4 + G1-chk + ST-1)**:
- **arm**: `docs_verify_emit.py`가 build 감지 시 센티널 기록. slug는 command 문자열에서 `(?:\.omd|outputs)/([^/\s'"]+)/` 첫 매치로 추출(HK-3 동형 알고리즘), 불명이면 전역 센티널. cwd는 `payload.get("cwd")` 우선, 없으면 `os.getcwd()`. 쓰기는 tmp 파일 + `os.replace()` (ST-1 atomic).
- **clear**: verify 신호(`pdftoppm` 또는 `unzip -t`)가 담긴 Bash 명령 감지 시 센티널 제거 — slug 판별되면 그 slug만, 불명이면 전부. `# ponytail: clearing heuristic is command-string sniffing; upgrade to explicit verify-stage signaling if false-clears surface`
- **enforce (Stop)**: `docs_stop_guard.py`가 미해소 센티널을 glob 스캔(`.omd/*/.verify-pending` + `.omd/.verify-pending`), advisory 리마인드. `stop_hook_active` true면 즉시 통과(G1-chk 재진입 가드). 6시간 이상 묵은 센티널은 "carried over from an earlier session"으로 구분 표시(HK-4 — TTL 자동 만료는 하지 않음). 문구에 "advisory — verify를 나중에 해도 무방" 명시(HK-2). `decision:block` 절대 금지(D6).

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_verify_emit.py`에 arm/clear 케이스 추가, Create `tests/test_stop_guard.py` — 기존 훅 테스트의 호출 패턴 재사용, `tmp_path`를 payload `cwd`로 주입:

```python
# test_verify_emit.py 추가 케이스 (tmp_path fixture 사용):
# a) build 명령 + command에 outputs/mydeck/ 경로 포함 → tmp_path/.omd/mydeck/.verify-pending 생성
# b) build 명령 + slug 단서 없음("python3 build_deck.py") → tmp_path/.omd/.verify-pending 생성
# c) "pdftoppm -png -r 150 deck.pdf out" 명령 → 기존 센티널 전부 제거, 리마인더 미발화
# d) 센티널 내용은 json이고 armed_at(epoch float)·command_head(str) 키 보유
# e) .omd 디렉토리 생성 불가(권한) 시에도 exit 0 (fail-open)

# test_stop_guard.py 케이스:
# 1) 센티널 0개 → 출력 없음, exit 0
# 2) .omd/mydeck/.verify-pending 존재 → 출력에 "mydeck"과 "advisory" 포함, "decision" 키 부재
# 3) payload {"stop_hook_active": true} → 센티널 있어도 출력 없음 (재진입 가드)
# 4) armed_at이 7시간 전인 센티널 → 출력에 "carried over" 포함
# 5) 깨진 stdin → exit 0 (fail-open)
```

Run: `python3 -m pytest tests/test_stop_guard.py tests/test_verify_emit.py -v`
Expected: 신규 케이스 전부 FAIL

- [ ] **Step 2: docs_verify_emit.py에 arm/clear 구현**

핵심 추가 (기존 리마인더 로직 앞단에 삽입):

```python
import os
import tempfile
import time

VERIFY_SIGNALS = ("pdftoppm", "unzip -t")
SLUG_RE = re.compile(r"(?:\.omd|outputs)/([^/\s'\"]+)/")
SENTINEL = ".verify-pending"


def _omd_root(payload):
    return os.path.join(payload.get("cwd") or os.getcwd(), ".omd")


def _sentinel_path(root, command):
    m = SLUG_RE.search(command)
    return os.path.join(root, m.group(1), SENTINEL) if m else os.path.join(root, SENTINEL)


def arm_sentinel(root, command):
    """Atomic write (ST-1): half-written sentinels must never exist."""
    path = _sentinel_path(root, command)
    os.makedirs(os.path.dirname(path), exist_ok=True)
    body = json.dumps({"armed_at": time.time(), "command_head": command[:80]})
    fd, tmp = tempfile.mkstemp(dir=os.path.dirname(path), prefix=".tmp-verify-")
    with os.fdopen(fd, "w") as f:
        f.write(body)
    os.replace(tmp, path)


def clear_sentinels(root, command):
    # ponytail: clearing heuristic is command-string sniffing; upgrade to explicit
    # verify-stage signaling if false-clears surface
    if not os.path.isdir(root):
        return  # defensive: callable safely outside main()'s try envelope
    m = SLUG_RE.search(command)
    targets = ([os.path.join(root, m.group(1), SENTINEL)] if m
               else [os.path.join(root, SENTINEL)]
               + [os.path.join(root, d, SENTINEL) for d in os.listdir(root)
                  if os.path.isdir(os.path.join(root, d))])
    for t in targets:
        try:
            os.remove(t)
        except OSError:
            pass
```

`main()` 개정 — build/verify 신호 분기 (전부 try 안, fail-open 유지):

```python
    if not is_doc_build(command):
        try:
            if any(sig in command for sig in VERIFY_SIGNALS):
                clear_sentinels(_omd_root(payload), command)
        except Exception:
            pass
        return 0

    try:
        arm_sentinel(_omd_root(payload), command)
    except Exception:
        pass  # fail-open: sentinel is best-effort, reminder still fires
```

- [ ] **Step 3: Stop 훅 구현**

구현 전 Stop 훅 출력 필드(`systemMessage`·`hookSpecificOutput.additionalContext`)를 Claude Code hooks 공식 문서와 대조 확인 — 둘 다 emit하는 이중화는 필드 지원 편차 대비이며, 어느 쪽이든 `decision` 키는 절대 쓰지 않는다(D6). Create `hooks/docs_stop_guard.py`:

```python
"""OMD Stop hook: advisory verify-pending reminder (G1). stdlib only, fail-open.

omd's #1 failure mode is "done" declared without a fresh render/verify. The
docs_verify_emit hook ARMS a `.omd/**/.verify-pending` sentinel on every document
build and CLEARS it when a verify-signal command runs; this hook, at Stop, lists
any sentinels still pending. Strictly advisory (D6): never `decision: block` —
deferring verify is legitimate, so the wording absorbs it (HK-2). Re-entry safe:
exits immediately when stop_hook_active (G1-chk).
"""
import json
import os
import sys
import time

SENTINEL = ".verify-pending"
STALE_AFTER = 6 * 3600  # older = carried over from an earlier session (HK-4; no TTL expiry)


def pending_sentinels(root: str):
    found = []
    top = os.path.join(root, SENTINEL)
    if os.path.isfile(top):
        found.append(("(slug unknown)", top))
    if os.path.isdir(root):
        for d in sorted(os.listdir(root)):
            p = os.path.join(root, d, SENTINEL)
            if os.path.isfile(p):
                found.append((d, p))
    return found


def build_message(items):
    lines = []
    now = time.time()
    for slug, path in items:
        try:
            armed = json.load(open(path)).get("armed_at", 0)
        except Exception:
            armed = 0
        tag = " — carried over from an earlier session" if now - armed > STALE_AFTER else ""
        lines.append(f"  - {slug}{tag}")
    return (
        "[OMD verify-pending] 이 세션에서 빌드된 문서 중 fresh verify가 아직 확인되지 않은 항목:\n"
        + "\n".join(lines)
        + "\n검증하려면 docs-verify(무결성 게이트+전수 정독). "
        "advisory입니다 — verify를 나중에 해도 무방하며, 의도적 유예라면 무시하고 진행하세요."
    )


def main() -> int:
    try:
        payload = json.load(sys.stdin)
        if payload.get("stop_hook_active"):
            return 0  # G1-chk: never re-fire inside a stop-hook continuation
        root = os.path.join(payload.get("cwd") or os.getcwd(), ".omd")
        items = pending_sentinels(root)
        if items:
            print(json.dumps({
                "systemMessage": build_message(items),
                "hookSpecificOutput": {
                    "hookEventName": "Stop",
                    "additionalContext": build_message(items),
                },
            }))
    except Exception:
        pass  # fail-open — a broken guard must never block Stop
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: plugin.json Stop 등록**

hooks 객체에 추가 (기존 항목과 동형):

```json
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python3",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/docs_stop_guard.py"]
          }
        ]
      }
    ]
```

- [ ] **Step 5: 테스트 통과 + 전체 suite**

Run: `python3 -m pytest tests/ -q`
Expected: 전부 green (신규 ~10케이스 포함)

- [ ] **Step 6: Commit**

```bash
git add hooks/docs_verify_emit.py hooks/docs_stop_guard.py .claude-plugin/plugin.json tests/test_stop_guard.py tests/test_verify_emit.py
git commit -m "feat(omd): verify-pending sentinel + advisory Stop guard handshake (G1)"
```

---

### Task 14: HG-3 — 리마인더 content-hash 쿨다운

**Files:**
- Modify: `hooks/docs_verify_emit.py`
- Test: `tests/test_verify_emit.py` (케이스 추가)

**Interfaces:**
- Consumes: T13까지 완성된 `docs_verify_emit.py`
- Produces: `.omd/.hook-throttle.json` 계약 — `{<sha256 hex>: <epoch float>}`

**설계**: 같은 리마인더 문구가 쿨다운(기본 600초, env `OMD_REMINDER_COOLDOWN_SECONDS` override) 내 재발화하면 **리마인더만 침묵** — 센티널 arm은 쿨다운과 무관하게 항상 수행(쿨다운은 메시지 소음 제어지 G1 상태 추적이 아님). 스로틀 파일은 세션 전역 단일(`.omd/.hook-throttle.json` — file_path 문맥 부재로 slug 단위 불가, spec HG-3). IO 실패 시 리마인더 발화 쪽으로 fail-open.

- [ ] **Step 1: 실패 테스트 추가**

`tests/test_verify_emit.py`에 케이스 추가 (tmp_path cwd 주입):

```python
# f) 같은 build 명령 2회 연속 → 1회차 리마인더 발화 + throttle 파일 생성, 2회차 출력 없음
#    (단 2회차에도 센티널은 갱신 arm — 쿨다운은 메시지만 침묵)
# g) OMD_REMINDER_COOLDOWN_SECONDS=0 이면 2회차도 발화 (쿨다운 해제)
# h) .omd/.hook-throttle.json이 깨진 JSON이어도 리마인더는 발화 (fail-open)
```

Run: 신규 3건 FAIL 확인

- [ ] **Step 2: 쿨다운 구현**

`hooks/docs_verify_emit.py`에 추가:

```python
import hashlib

THROTTLE_FILE = ".hook-throttle.json"
DEFAULT_COOLDOWN = 600.0


def _cooldown_seconds():
    try:
        return float(os.environ.get("OMD_REMINDER_COOLDOWN_SECONDS", DEFAULT_COOLDOWN))
    except ValueError:
        return DEFAULT_COOLDOWN


def reminder_throttled(root: str, message: str) -> bool:
    """True → suppress this reminder (same content within cooldown). Fail-open: any
    IO/JSON error answers False (fire the reminder — silence is the risky side)."""
    cooldown = _cooldown_seconds()
    if cooldown <= 0:
        return False
    path = os.path.join(root, THROTTLE_FILE)
    digest = hashlib.sha256(message.encode("utf-8")).hexdigest()
    now = time.time()
    try:
        with open(path, encoding="utf-8") as f:
            seen = json.load(f)
    except Exception:
        seen = {}
    throttled = isinstance(seen, dict) and now - float(seen.get(digest, 0) or 0) < cooldown
    if not throttled:
        try:
            seen = seen if isinstance(seen, dict) else {}
            seen[digest] = now
            fd, tmp = tempfile.mkstemp(dir=root, prefix=".tmp-throttle-")
            with os.fdopen(fd, "w") as f:
                json.dump(seen, f)
            os.replace(tmp, path)
        except Exception:
            pass  # recording failure must not suppress the reminder
    return throttled
```

`main()`의 리마인더 출력 직전에 (arm 이후):

```python
    reminder = build_reminder()
    try:
        if reminder_throttled(_omd_root(payload), reminder):
            return 0
    except Exception:
        pass  # fail-open: on any doubt, fire the reminder
```

- [ ] **Step 3: 테스트 통과 + 전체 suite**

Run: `python3 -m pytest tests/ -q` → 전부 green

- [ ] **Step 4: Commit**

```bash
git add hooks/docs_verify_emit.py tests/test_verify_emit.py
git commit -m "feat(omd): content-hash cooldown for verify reminder (HG-3)"
```

---

### Task 15: Release v0.1.0

**Files:**
- Modify: `.claude-plugin/plugin.json` (version bump)
- Modify: `CHANGELOG.md` (0.1.0 절 작성)
- Modify: `README.md` (정합 재확인 — 필요 시)

**Interfaces:**
- Consumes: T1–T14 전체
- Produces: v0.1.0 릴리스 상태 (LOCAL ONLY — merge·tag는 사용자 게이트)

- [ ] **Step 1: version bump**

`.claude-plugin/plugin.json`: `"version": "0.0.0"` → `"version": "0.1.0"`

- [ ] **Step 2: CHANGELOG 0.1.0 절 작성**

`## [Unreleased]`의 기존 내용 전체를 `## [0.1.0] - 2026-07-13` 아래로 이동하고(semver 이전 이력이 v0.1.0에 최초 수록됨), R1 변경분을 keep-a-changelog 6유형으로 앞에 추가. `## [Unreleased]`는 빈 상태로 최상단 유지:

```markdown
## [Unreleased]

## [0.1.0] - 2026-07-13

R1 "hygiene + core gates" — first semver release (release train: spec
`docs/superpowers/specs/2026-07-11-omd-program-design.md` §5).

### Added
- Version SSOT: `plugin.json` `version` field + `tests/test_version_sync.py` (H3).
- Distribution-axiom guard: `tests/test_distribution_axiom.py` — no personal home paths /
  emails in shipped files, pattern-based per spec DT-3 (D8).
- Skill context budget guard: `tests/test_skill_budget.py`, 100 KiB cap (IA-1).
- Format-card authoring contract `references/formats/README.md` — required sections,
  VERIFIED stamp grammar, **engine-drift demotion rule** (`UNVERIFIED (engine drift)`),
  enforced by `tests/test_format_cards.py`; builder/verifier now run an engine-version
  pin check (G7).
- Agent contract guard `tests/test_agent_contract.py`: frontmatter model policy +
  Final_Response_Contract markers + self-approval bans (AC-1a).
- **model-guard hook** `hooks/model_guard.py` (PreToolUse Task): advisory warning on
  explicit model overrides contradicting agent frontmatter, and on unknown
  `oh-my-docs:*` agent names (G4).
- **verify-pending handshake** (G1): `docs_verify_emit` arms `.omd/**/.verify-pending`
  on document builds and clears it on verify signals; new Stop hook
  `hooks/docs_stop_guard.py` lists still-pending documents at Stop — strictly advisory,
  re-entry-safe (`stop_hook_active`), stale sentinels marked "carried over", never blocks.
- Reminder cooldown: same-content verify reminders are throttled for 10 min
  (`.omd/.hook-throttle.json`, `OMD_REMINDER_COOLDOWN_SECONDS` override) (HG-3).

### Changed
- **Versioning policy**: commit-SHA versioning → Keep a Changelog + SemVer (this file's
  header note; user-ratified via R1 PR).
- `doc-planner` frontmatter model opus → **sonnet**; Deliberate `--consensus` escalates
  to opus at Task-call time in `docs-plan` (G4 precondition, spec critique #2).
- Routing checkpoint pins **pdf as input/convert layer** (never a generation FORMAT) +
  regression test (H5 decision).
- README Status/Structure refreshed to the current inventory (H6).

### Fixed
- `docs-pdf` skill registered in plugin.json — was shipping dead (H1).
- `docs-pilot` bilingual "식별자 스크럽" confidentiality anchor restored (H2).
- `docs_verify_emit`: dead `DOC_EXTS` constant removed; xlsx engine signals
  (openpyxl/xlsxwriter/Workbook() + xlsx-named scripts) now trigger the reminder (H4).
- `references/themes/` fallback presets wired into docs-build / docs-standardize (H7).
```

- [ ] **Step 3: README 정합 재확인**

Run: `grep -n "0\.1\.0\|2026-07-13" README.md` — T7이 쓴 Status 절과 릴리스 내용 일치 확인, 불일치 시 수정.

- [ ] **Step 4: 전체 suite 최종 green + 결함 패턴 스캔**

Run: `python3 -m pytest tests/ -q`
Expected: 전부 PASS (예상 ~55+, 0 failed)

Run: `grep -rn "TODO\|TBD\|test.skip\|@skip" hooks/ tests/ --include="*.py"`
Expected: 0건 (fake-completion 가드)

- [ ] **Step 5: Commit**

```bash
git add .claude-plugin/plugin.json CHANGELOG.md README.md
git commit -m "feat(omd): release v0.1.0 — hygiene + core gates (R1)"
```

---

## Self-Review 결과 (plan 작성 세션)

1. **Spec 커버리지**: §5 R1 행의 모든 항목 매핑 — H1(T1)·H2(T2)·H3+CHANGELOG정책(T3)·H4(T4)·H5(T5)·H6(T7)·H7(T6)·버전SSOT+sync테스트(T3)·distribution-axiom(T8)·G1(T13)·G4(T12)·G7(T9)·model정합감사(T10)·AC-1a(T11)·IA-1(T8)·HG-3(T14)·G1-chk 재진입가드(T13 Step3)·ST-1 atomic write(T13 Step2, T14 Step2)·Release(T15). build-order 준수: T1·T2(green) → T3(SSOT) → T4~T7(위생) → T8(신규 테스트) → T9(G7 먼저) → T10~T12(G4 전제→G4) → T13(G1) → T14 → T15.
2. **Placeholder 스캔**: T5 Step1·T12 Step1·T13 Step1의 "기존 헬퍼 재사용" 지시는 저장소에 실존하는 코드 참조(구현자가 해당 테스트 파일을 먼저 정독) — 미정의 심볼 참조 아님.
3. **타입 일관성**: 센티널 JSON 키(`armed_at`·`command_head`)는 T13 정의·T13 테스트 소비 일치. `EXPECTED_MODELS`(T10)와 model_guard frontmatter 파싱(T12)은 같은 frontmatter 형식 소비. `_omd_root()`는 T13 정의·T14 재사용.
