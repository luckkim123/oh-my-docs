# omd v0.4.0 R4 — 지식 수명주기 (capture-then-curate 완성) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** omd의 학습 루프에서 부재한 capture 절반을 완성하고(G2 notepad·G3 wiki lint·G5 stage evidence·G6 ownership guard·OBS capture), wiki README가 이미 약속한 CJK bi-gram 검색 계약(KN-2)과 경로/슬러그 안전장치(KN-3·KN-4·ST-3)를 이행한다 — 릴리스 트레인(R1~R4)의 마지막 라운드.

**Architecture:** 신규 코드 3파일(`references/wiki/query_helper.py`·`references/wiki/lint_wiki.py`·`hooks/precompact_reinject.py`, 전부 stdlib-only)과 기존 stop guard 확장 1건. 나머지는 스킬/카드 prose 배선(계약을 pytest pin으로 고정). 모든 게이트는 advisory+fail-open(D6), lint/capture는 report-only·human-gate 유지.

**Tech Stack:** Python 3 stdlib only (re/unicodedata/hashlib/os/json/argparse). pytest (기존 136 green 기준선). 외부 의존성 추가 0.

**Spec:** `docs/superpowers/specs/2026-07-11-omd-program-design.md` §3.1(G2·G3·G5·G6)·§3.4(KN-2·KN-3·ST-3·KN-4)·§4.5·§5 R4행.

## Global Constraints

- **stdlib-only**: hooks/·references/ 내 모든 .py는 Python 표준 라이브러리만 (spec §7 배포 경계).
- **fail-open advisory**: 훅은 예외 시 조용히 통과, `decision: block` 금지 (D6). Stop 훅은 `stop_hook_active` 재진입 가드 유지 (G1-chk).
- **report-only lint**: lint는 절대 auto-fix 하지 않고 exit 0 (omx W5 승계). 판단은 사람/docs-learn 몫.
- **no embeddings, ever**: wiki 검색은 결정론적 keyword matching만 (영구 불변식).
- **FORM-only 학습**: content(텍스트·주장·수치·출처)는 OBS/wiki 승격 영구 금지 (§6.F). 승격은 human gate만 (heavy-channel invariant).
- **append-only ledger**: `.omd/learned.md`는 append + 재관찰 시 evidence 갱신만. 블록 삭제/retire는 docs-learn 전용.
- **atomic write (ST-1)**: 훅이 파일을 변경할 때는 같은 디렉토리 tmp 파일 + `os.replace()`. open+truncate 금지.
- **distribution axiom**: 배포 파일에 개인 식별자·절대경로 금지 (`test_distribution_axiom.py`가 강제 — 새 파일도 스캔 대상).
- **skill budget**: `skills/*/SKILL.md` 합계 100KiB 상한 (`test_skill_budget.py`). prose 추가는 간결하게.
- **버전 SSOT**: `.claude-plugin/plugin.json` version, semver (`test_version_sync.py`).
- **worktree**: `/Users/kimseungmin/.claude/worktrees/oh-my-docs-r4` (branch `feat/v0.4.0-r4`). 커밋 컨벤션 `feat(omd):`/`test(omd):`/`docs(omd):`.
- 기준선: main @ 72890ef, **pytest 136 passed** — 각 태스크 후 전체 suite green 유지.

---

## Plan-level decisions (spec이 plan 층에 위임한 결정 — critic 검토 대상)

| # | 결정 | 근거 |
|:--|:---|:---|
| PD-1 | **G2 재주입은 SessionStart(matcher `compact`)로, pruning은 PreCompact로** — 한 파일(`hooks/precompact_reinject.py`) 두 등록, `hook_event_name`으로 분기 | Claude Code에서 `additionalContext` 주입은 UserPromptSubmit/SessionStart 계열이 지원 — PreCompact 출력은 컨텍스트 주입 채널이 아니다. compaction 후 SessionStart(source=compact)가 발화하므로 "compaction 생존" 의미는 동일하게 달성. 파일명은 spec 명명(`precompact_reinject.py`) 유지 |
| PD-2 | **lint는 schema-less 스토어에 적응**: frontmatter 요구 없음(옵션 `confidence:`만 검사), stale은 파일 mtime 기준(`--now` 주입 가능), orphan은 스토어에 [[링크]] 문화가 실재할 때만 발화 | omd wiki는 "free-form .md, no machine-parse schema" 계약(README §layout). 링크 문화 없는 스토어에서 orphan은 전 파일 오탐 |
| PD-3 | **G5 증거 채널 = `.omd/<slug>/stage-evidence.log`** (pilot이 append), stop guard는 mtime이 STALE_AFTER(6h) 이내인 로그만 검사 | Stop 훅은 세션 상태 저장소가 없으므로(HK-1 선례) 파일 mtime이 "이번 세션" 근사. 이전 세션 로그를 소급 경고하지 않음 |
| PD-4 | **G6은 prose 절차 + pytest pin** — 새 helper 스크립트 없음. LLM이 `shasum -a 256`으로 manifest 대조 | manifest.json(D4)과 명령이 이미 존재. 결정론 코드가 필요한 건 대조 규칙이 아니라 manifest 원자쓰기(ST-1, R2에서 완료) |
| PD-5 | **capture는 훅이 아니라 스킬 prose 스텝** — pilot Step 7b + inspect/verify/standardize 각 1스텝. 결정론이 필요한 쓰기 보조(슬러그·경로)만 query_helper가 담당 | "form 패턴이 반복됐는가"는 3초 예산 stdlib 훅이 판단 불가. OMC capture 철학(값싸고 자동)은 "관찰한 것만, 추측 금지, 몇 줄 이내" 규율로 이식 |
| PD-6 | **query_helper CLI = `tokenize`/`match`/`slug` 3동사**, match 점수 = 토큰 겹침 수(tf-idf 없음) | YAGNI — 소비자는 관련 노트 상위 몇 개를 찾는 grep 보조. LLM grep은 여전히 유효한 degrade 경로 |

## File map (전체 산출·수정 대상)

| 구분 | 경로 | 태스크 |
|:--|:---|:--|
| Create | `references/wiki/query_helper.py` | 1·2 |
| Create | `tests/test_wiki_query_helper.py` | 1·2 |
| Modify | `references/wiki/README.md` (KN-2 포인터·ST-3·KN-4) | 3 |
| Modify | `tests/test_wiki_two_level.py` (계약 pin 추가) | 3 |
| Create | `references/wiki/lint_wiki.py` | 4 |
| Create | `tests/test_wiki_lint.py` | 4 |
| Modify | `skills/docs-learn/SKILL.md` (lint 호출 배선) | 5 |
| Modify | `tests/test_skill_contract.py` (docs-learn pin) | 5 |
| Create | `hooks/precompact_reinject.py`, `references/notepad.md` | 6 |
| Create | `tests/test_precompact_reinject.py` | 6 |
| Modify | `.claude-plugin/plugin.json` (PreCompact·SessionStart 등록) | 6 |
| Modify | `skills/docs-pilot/SKILL.md` (notepad 포인터·G5 마커·Step 7b·G6) | 6·7·8·9 |
| Modify | `hooks/docs_stop_guard.py` + `tests/test_stop_guard.py` (G5) | 7 |
| Modify | `references/output-layout.md` §3.4 + `tests/test_output_layout_contract.py` (G6) | 8 |
| Modify | `skills/docs-build/SKILL.md`·`skills/docs-revise/SKILL.md`·`agents/doc-builder.md` (G6 한 줄) | 8 |
| Modify | `skills/docs-inspect/SKILL.md`·`skills/docs-verify/SKILL.md`·`skills/docs-standardize/SKILL.md` (capture 스텝) | 9 |
| Modify | `references/learning-protocol.md` (capture-then-curate·재관찰 semantics) | 9 |
| Modify | `.claude-plugin/plugin.json` version·`CHANGELOG.md`·`README.md` | 10 |

---

### Task 1: query_helper.py — CJK bi-gram 토크나이저 + match (KN-2, README 계약 이행)

**Files:**
- Create: `references/wiki/query_helper.py`
- Test: `tests/test_wiki_query_helper.py`

**Interfaces:**
- Produces: `tokenize(text: str) -> set[str]` (3단: Latin 어절 / CJK 1+2-gram / 기타 폴백), `match(store_dir: str, query: str, limit: int = 10) -> list[tuple[str, int]]` (파일명, 겹침수 — 겹침 내림차순·이름 오름차순), CLI `python3 query_helper.py tokenize <text>` / `match <category-dir> <text>`.
- Consumes: 없음 (신규 모듈, stdlib only).

**배경 (구현자 필독):** `references/wiki/README.md:89`가 "Deterministic grep only (keyword matching, incl. CJK bi-gram)"을 이미 계약으로 명시했으나 구현 코드가 없다 — 이 태스크는 신규 기능이 아니라 **약속 이행**이다(spec §3.4 KN-2). 훅 미등록 선택 유틸: 어느 스테이지든 Bash로 shell-out 할 수 있고, python3가 없으면 LLM grep이 degrade 경로.

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_wiki_query_helper.py
"""KN-2·KN-3·KN-4 — references/wiki/query_helper.py contract tests."""
import os
import subprocess
import sys
import unittest

HELPER_DIR = os.path.join(os.path.dirname(__file__), "..", "references", "wiki")
sys.path.insert(0, HELPER_DIR)

import query_helper as qh  # noqa: E402


class TestTokenize(unittest.TestCase):
    def test_latin_words_lowercased(self):
        toks = qh.tokenize("Caption Style 12pt")
        self.assertTrue({"caption", "style", "12pt"} <= toks)

    def test_cjk_one_and_two_grams(self):
        toks = qh.tokenize("디펜스 자료")
        # 1-grams
        self.assertTrue({"디", "펜", "스", "자", "료"} <= toks)
        # 2-grams (어절 내부만 — 공백을 건너는 bigram 없음)
        self.assertTrue({"디펜", "펜스", "자료"} <= toks)
        self.assertNotIn("스자", toks)

    def test_mixed_scripts(self):
        toks = qh.tokenize("IEEE 논문 rule")
        self.assertTrue({"ieee", "rule", "논", "문", "논문"} <= toks)

    def test_empty_and_none(self):
        self.assertEqual(qh.tokenize(""), set())
        self.assertEqual(qh.tokenize(None), set())


class TestMatch(unittest.TestCase):
    def _store(self, tmp):
        os.makedirs(tmp, exist_ok=True)
        with open(os.path.join(tmp, "defense-defect-patterns.md"), "w", encoding="utf-8") as f:
            f.write("# defense deck defect patterns\n디펜스 발표의 캡션 결함 반복\n")
        with open(os.path.join(tmp, "lab-style-spec.md"), "w", encoding="utf-8") as f:
            f.write("# lab style spec\ncaption 12pt black\n")
        with open(os.path.join(tmp, "not-markdown.txt"), "w", encoding="utf-8") as f:
            f.write("디펜스\n")  # .md 아님 — 매칭 대상 제외

    def test_ranks_by_overlap(self):
        import tempfile
        with tempfile.TemporaryDirectory() as tmp:
            self._store(tmp)
            hits = qh.match(tmp, "디펜스 캡션 결함")
            self.assertEqual(hits[0][0], "defense-defect-patterns.md")
            names = [n for n, _ in hits]
            self.assertNotIn("not-markdown.txt", names)

    def test_missing_dir_returns_empty(self):
        self.assertEqual(qh.match("/nonexistent/dir/for/omd-test", "질의"), [])

    def test_cli_match_smoke(self):
        import tempfile
        with tempfile.TemporaryDirectory() as tmp:
            self._store(tmp)
            out = subprocess.run(
                [sys.executable, os.path.join(HELPER_DIR, "query_helper.py"),
                 "match", tmp, "디펜스 캡션"],
                capture_output=True, text=True)
            self.assertEqual(out.returncode, 0)
            self.assertIn("defense-defect-patterns.md", out.stdout)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 실패 확인**

Run: `cd /Users/kimseungmin/.claude/worktrees/oh-my-docs-r4 && python3 -m pytest tests/test_wiki_query_helper.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'query_helper'`

- [ ] **Step 3: 최소 구현**

```python
# references/wiki/query_helper.py
"""omd wiki query helper (KN-2·KN-3·KN-4) — stdlib-only OPTIONAL utility, NOT a registered hook.

Fulfils the CJK bi-gram keyword-matching contract references/wiki/README.md has
promised (a contract-debt repayment, not a new feature). Three-tier tokenizer:
Latin words / CJK 1+2-grams / other-script whitespace fallback. Deterministic
only — no embeddings, ever (hard invariant). Any stage may shell out:
    python3 query_helper.py tokenize <text>
    python3 query_helper.py match <category-dir> <text>
    python3 query_helper.py slug <title>
Plain LLM grep stays a valid degrade path when python3 is unavailable.
"""
import hashlib
import os
import re
import sys
import unicodedata

_LATIN = re.compile(r"[a-z0-9]+")
# Hangul syllables + jamo + compat jamo, CJK unified ideographs, Hiragana/Katakana
# (escaped ranges only — literal CJK range chars invite encoding bugs)
_CJK = re.compile(
    "[" "\\uac00-\\ud7a3"   # Hangul syllables
        "\\u1100-\\u11ff"   # Hangul jamo
        "\\u3130-\\u318f"   # Hangul compat jamo
        "\\u4e00-\\u9fff"   # CJK unified ideographs
        "\\u3040-\\u30ff"   # Hiragana + Katakana
    "]+")


def tokenize(text):
    """3-tier deterministic token set: latin words / CJK 1+2-grams / other fallback."""
    text = unicodedata.normalize("NFKC", text or "").lower()
    tokens = set(_LATIN.findall(text))
    for run in _CJK.findall(text):
        tokens.update(run)  # 1-grams
        tokens.update(run[i:i + 2] for i in range(len(run) - 1))  # 2-grams
    leftover = _CJK.sub(" ", _LATIN.sub(" ", text))
    tokens.update(t for t in leftover.split() if t)
    return tokens


def match(store_dir, query, limit=10):
    """Rank a category dir's .md files by token overlap with the query.
    Deterministic (overlap desc, then name asc); [] when the dir is absent."""
    want = tokenize(query)
    if not want or not os.path.isdir(store_dir):
        return []
    scored = []
    for name in sorted(os.listdir(store_dir)):
        if not name.endswith(".md"):
            continue
        try:
            with open(os.path.join(store_dir, name), encoding="utf-8",
                      errors="replace") as f:
                body = f.read()
        except OSError:
            continue
        overlap = len(want & tokenize(name + "\n" + body))
        if overlap:
            scored.append((-overlap, name))
    return [(name, -neg) for neg, name in sorted(scored)[:limit]]


def main(argv):
    if len(argv) >= 2 and argv[0] == "tokenize":
        print("\n".join(sorted(tokenize(" ".join(argv[1:])))))
        return 0
    if len(argv) >= 3 and argv[0] == "match":
        for name, score in match(argv[1], " ".join(argv[2:])):
            print(f"{score}\t{name}")
        return 0
    print("usage: query_helper.py tokenize <text> | match <dir> <text> | slug <title>",
          file=sys.stderr)
    return 2


if __name__ == "__main__":
    sys.exit(main(sys.argv[1:]))
```

(`slug` 동사는 Task 2에서 추가 — usage 문자열은 지금 미리 포함해 둔다.)

- [ ] **Step 4: 통과 확인** — Run: `python3 -m pytest tests/test_wiki_query_helper.py -v` → PASS. 전체: `python3 -m pytest -q` → 기존 136 + 신규 전부 green.

- [ ] **Step 5: Commit** — `git add references/wiki/query_helper.py tests/test_wiki_query_helper.py && git commit -m "feat(omd): wiki query helper — CJK bi-gram tokenizer + match (KN-2, README contract repayment)"`

---

### Task 2: safe_wiki_path + title_to_slug (KN-3·KN-4)

**Files:**
- Modify: `references/wiki/query_helper.py` (함수 2개 + CLI `slug` 동사 추가)
- Modify: `tests/test_wiki_query_helper.py` (테스트 클래스 2개 추가)

**Interfaces:**
- Produces: `safe_wiki_path(wiki_root: str, category: str, filename: str) -> str` (불량 세그먼트 시 `ValueError`), `title_to_slug(title: str) -> str` (항상 `.md`로 끝나는 영문 슬러그; ASCII 토큰 0개면 `note-<sha256 8hex>.md` 결정론 폴백 — **해시 폴백은 이 함수 안에만 존재**, LLM 즉석 해시 금지가 KN-4의 요지).
- Consumes: Task 1의 모듈 골격.

- [ ] **Step 1: 실패하는 테스트 추가**

```python
# tests/test_wiki_query_helper.py 에 추가
class TestSafeWikiPath(unittest.TestCase):
    def test_accepts_normal(self):
        import tempfile
        with tempfile.TemporaryDirectory() as tmp:
            p = qh.safe_wiki_path(tmp, "convention", "lab-style-spec.md")
            self.assertTrue(p.endswith(os.path.join("convention", "lab-style-spec.md")))

    def test_rejects_traversal_shapes(self):
        for cat, name in [("convention", "../escape.md"),
                          ("convention", "a/b.md"),
                          ("convention", "a\\b.md"),
                          ("..", "x.md"),
                          ("convention", ".hidden.md"),
                          ("", "x.md")]:
            with self.assertRaises(ValueError, msg=f"{cat!r}/{name!r}"):
                qh.safe_wiki_path("/tmp/omd-wiki-root", cat, name)


class TestTitleToSlug(unittest.TestCase):
    def test_english_keywords(self):
        self.assertEqual(qh.title_to_slug("Defense deck defect patterns"),
                         "defense-deck-defect-patterns.md")

    def test_mixed_keeps_ascii_only(self):
        self.assertEqual(qh.title_to_slug("IEEE 캡션 rule"), "ieee-rule.md")

    def test_pure_korean_falls_back_to_hash(self):
        a = qh.title_to_slug("디펜스 자료 결함")
        b = qh.title_to_slug("디펜스 자료 결함")
        self.assertEqual(a, b)  # 결정론
        self.assertRegex(a, r"^note-[0-9a-f]{8}\.md$")

    def test_cli_slug(self):
        out = subprocess.run(
            [sys.executable, os.path.join(HELPER_DIR, "query_helper.py"),
             "slug", "Defense deck defect patterns"],
            capture_output=True, text=True)
        self.assertEqual(out.stdout.strip(), "defense-deck-defect-patterns.md")
```

- [ ] **Step 2: 실패 확인** — Run: `python3 -m pytest tests/test_wiki_query_helper.py -v` → FAIL (`AttributeError: ... 'safe_wiki_path'`)

- [ ] **Step 3: 구현**

```python
# references/wiki/query_helper.py 에 추가 (tokenize 아래)
def safe_wiki_path(wiki_root, category, filename):
    """KN-3: validate a wiki write target BEFORE any Write into .omd/wiki/**.
    Rejects separators, '..' and dot-prefixed segments, then re-checks the
    resolved path still sits under wiki_root (belt and braces)."""
    for part in (category, filename):
        if (not part or "/" in part or "\\" in part or ".." in part
                or part.startswith(".")):
            raise ValueError(f"unsafe wiki path segment: {part!r}")
    root = os.path.realpath(wiki_root)
    target = os.path.realpath(os.path.join(root, category, filename))
    if os.path.commonpath([root, target]) != root:
        raise ValueError(f"resolved path escapes wiki root: {target}")
    return os.path.join(wiki_root, category, filename)


def title_to_slug(title):
    """KN-4: English-keyword slug even for Korean topics (paraphrase to English
    keywords upstream when possible). No ASCII token at all -> deterministic
    sha256 8-hex fallback. The hash fallback lives HERE, in write code — an LLM
    must never improvise a hash (non-deterministic across sessions)."""
    words = re.findall(r"[a-z0-9]+", unicodedata.normalize("NFKC", title or "").lower())
    slug = "-".join(words)[:64].strip("-")
    if slug:
        return slug + ".md"
    digest = hashlib.sha256((title or "").encode("utf-8")).hexdigest()[:8]
    return f"note-{digest}.md"
```

CLI `main()`에 추가 (match 분기 아래):

```python
    if len(argv) >= 2 and argv[0] == "slug":
        print(title_to_slug(" ".join(argv[1:])))
        return 0
```

- [ ] **Step 4: 통과 확인** — `python3 -m pytest tests/test_wiki_query_helper.py -v` → PASS, 전체 suite green.
- [ ] **Step 5: Commit** — `git commit -am "feat(omd): safe_wiki_path + title_to_slug — wiki write guards (KN-3, KN-4)"`

---

### Task 3: wiki README 계약 갱신 — query_helper 포인터 · ascent $HOME 하한(ST-3) · 영문 슬러그 규칙(KN-4)

**Files:**
- Modify: `references/wiki/README.md`
- Modify: `tests/test_wiki_two_level.py` (pin 3개 추가)

**Interfaces:**
- Consumes: Task 1·2의 `query_helper.py` (포인터 대상).
- Produces: README 계약 문장 3건 — 이후 태스크·향후 세션이 SSOT로 읽는다.

- [ ] **Step 1: 실패하는 pin 테스트 추가**

```python
# tests/test_wiki_two_level.py 에 추가 (기존 헬퍼/상수 재사용 — 파일 상단의 README 로드 방식을 그대로 따를 것)
def test_ascent_stops_at_home(readme_text=None):
    """ST-3: ascent must never climb above $HOME — a shallow tree must not merge
    unrelated projects' stores through an accidental common ancestor."""
    text = readme_text or open(WIKI_README, encoding="utf-8").read()
    assert "$HOME" in text and "never" in text.lower()

def test_english_slug_rule_stated():
    """KN-4: filenames are English-keyword slugs even for Korean topics."""
    text = open(WIKI_README, encoding="utf-8").read()
    assert "English-keyword" in text and "title_to_slug" in text

def test_query_helper_pointer_present():
    """KN-2: the promised CJK bi-gram matching now points at its implementation."""
    text = open(WIKI_README, encoding="utf-8").read()
    assert "query_helper.py" in text
```

(⚠️ 기존 `tests/test_wiki_two_level.py`의 README 경로 상수·로드 패턴을 먼저 읽고 그 방식을 따를 것 — 위 코드의 `WIKI_README`는 그 파일에 이미 있는 상수를 재사용하라는 뜻이며, 없으면 같은 이름으로 정의한다.)

- [ ] **Step 2: 실패 확인** — `python3 -m pytest tests/test_wiki_two_level.py -v` → 신규 3개 FAIL.

- [ ] **Step 3: README 수정 3건**

(a) `## wiki_query(category) abstract-function contract` 절의 "Deterministic grep only (keyword matching, incl. CJK bi-gram)." 문장을 다음으로 교체:

```markdown
  Deterministic grep only (keyword matching, incl. CJK bi-gram — implemented by
  `references/wiki/query_helper.py`; shell out `python3 <plugin>/references/wiki/query_helper.py
  match <category-dir> "<query>"` when python3 is available, plain grep as the degrade path).
```

(b) ascent를 서술하는 두 곳(디렉토리 layout의 `discovery = ascent …` 줄과 contract의 `parent_omd = ascent(...)` 줄) 중 **layout 쪽 줄 바로 아래**에 추가:

```markdown
        │   ⚠️ the ascent never climbs above `$HOME` (ST-3) — the home directory is the hard
        │   lower bound, so unrelated projects can never merge through an accidental
        │   common ancestor above it (the confidentiality gate stays intact on shallow trees).
```

(c) `## Directory layout` 절의 "One file = one topic" 불릿을 다음으로 교체:

```markdown
- One file = one topic (e.g. `convention/defense-defect-patterns.md`). Filenames are
  **English-keyword slugs even when the topic is Korean** (paraphrase to English keywords —
  KN-4). When writing via code, use `query_helper.title_to_slug()` — it owns the deterministic
  hash fallback; never improvise a hash inline. Validate the target with
  `query_helper.safe_wiki_path()` before any Write into `.omd/wiki/**` (KN-3).
```

- [ ] **Step 4: 통과 확인** — `python3 -m pytest tests/test_wiki_two_level.py -v` → PASS, 전체 suite green.
- [ ] **Step 5: Commit** — `git commit -am "docs(omd): wiki README — query_helper pointer, ascent \$HOME floor (ST-3), English slug rule (KN-4)"`

---

### Task 4: lint_wiki.py — wiki 기계 lint, report-only (G3)

**Files:**
- Create: `references/wiki/lint_wiki.py`
- Test: `tests/test_wiki_lint.py`

**Interfaces:**
- Produces: `scan(root, now=None, stale_days=90, max_bytes=10240) -> list[tuple[severity, type, relpath, message]]`, CLI `python3 lint_wiki.py [--root .omd/wiki] [--now ISO] [--stale-days N] [--max-bytes N]` — 사람이 읽는 리포트 stdout, **exit 항상 0** (report-only, W5).
- Consumes: 없음 (stdlib). omx `omx_core/wiki/lint.py`의 ADAPT — omd 차이는 PD-2 참조.

**체크 목록 (PD-2 적응):**

| type | severity | 조건 |
|:--|:--|:---|
| `misplaced` | warning | 카테고리 디렉토리 밖(루트 직속)의 .md |
| `unknown-category` | warning | `convention/pattern/decision/reference` 밖의 하위 디렉토리 |
| `stale` | info | mtime이 `--now`(기본 wall clock) 기준 stale_days 초과 |
| `oversized` | warning | 파일 크기 > max_bytes (기본 10240B) |
| `broken-ref` | warning | `[[target]]`이 스토어 내 어떤 파일 stem과도 불일치 |
| `orphan` | info | 스토어에 [[링크]]가 하나라도 있을 때만: inbound 0 && fresh 아님(stale_days/2) |
| `near-duplicate` | info | 같은 카테고리 내 파일명 토큰 Jaccard ≥ 0.5 (omx 실측 근거 차용) |
| `missing-confidence` | info | `convention/*.md`에 `confidence:` 마커 부재 (README frontmatter 권고) |

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_wiki_lint.py
"""G3 — references/wiki/lint_wiki.py: report-only auditor for .omd/wiki stores."""
import os
import subprocess
import sys
import tempfile
import time
import unittest

HELPER_DIR = os.path.join(os.path.dirname(__file__), "..", "references", "wiki")
sys.path.insert(0, HELPER_DIR)

import lint_wiki  # noqa: E402

NOW = "2026-07-14T00:00:00"


def build_store(root):
    """Fixture: one healthy note, one stale schema-less note, one oversized,
    one broken-ref, one near-dup pair, one misplaced file, one unknown dir."""
    conv = os.path.join(root, "convention")
    dec = os.path.join(root, "decision")
    os.makedirs(conv); os.makedirs(dec)
    os.makedirs(os.path.join(root, "scratch"))          # unknown-category
    w = lambda p, s: open(p, "w", encoding="utf-8").write(s)
    w(os.path.join(root, "loose.md"), "misplaced\n")     # misplaced
    w(os.path.join(conv, "lab-style-spec.md"),
      "---\nconfidence: high\n---\n# lab style\ncaption 12pt, see [[defense-defect-patterns]]\n")
    w(os.path.join(conv, "defense-defect-patterns.md"),
      "# defense defects\nno confidence marker here\n")  # missing-confidence
    w(os.path.join(conv, "defense-defect-patterns-two.md"),
      "# near dup of the above\n")                       # near-duplicate + missing-confidence
    w(os.path.join(dec, "big-note.md"), "x" * 11000)     # oversized
    w(os.path.join(dec, "arc-choice.md"), "see [[no-such-page]]\n")  # broken-ref
    old = time.mktime(time.strptime("2026-01-01", "%Y-%m-%d"))
    os.utime(os.path.join(conv, "defense-defect-patterns.md"), (old, old))  # stale


class TestScan(unittest.TestCase):
    def types(self, issues):
        return {t for _, t, _, _ in issues}

    def test_detects_expected_issue_types(self):
        with tempfile.TemporaryDirectory() as tmp:
            build_store(tmp)
            issues = lint_wiki.scan(tmp, now=NOW)
            self.assertTrue({"misplaced", "unknown-category", "stale", "oversized",
                             "broken-ref", "near-duplicate", "missing-confidence"}
                            <= self.types(issues))

    def test_orphan_only_with_link_culture(self):
        with tempfile.TemporaryDirectory() as tmp:
            conv = os.path.join(tmp, "convention"); os.makedirs(conv)
            open(os.path.join(conv, "solo-note.md"), "w").write("no links anywhere\n")
            old = time.mktime(time.strptime("2026-01-01", "%Y-%m-%d"))
            os.utime(os.path.join(conv, "solo-note.md"), (old, old))
            # 링크 문화 없음 → orphan 미발화
            self.assertNotIn("orphan", self.types(lint_wiki.scan(tmp, now=NOW)))

    def test_missing_root_is_single_info(self):
        issues = lint_wiki.scan("/nonexistent/omd-wiki", now=NOW)
        self.assertEqual([i[1] for i in issues], ["empty"])
        self.assertEqual(issues[0][0], "info")

    def test_cli_reports_and_exits_zero(self):
        with tempfile.TemporaryDirectory() as tmp:
            build_store(tmp)
            out = subprocess.run(
                [sys.executable, os.path.join(HELPER_DIR, "lint_wiki.py"),
                 "--root", tmp, "--now", NOW],
                capture_output=True, text=True)
            self.assertEqual(out.returncode, 0)   # report-only: 이슈가 있어도 0
            self.assertIn("near-duplicate", out.stdout)
            self.assertIn("total", out.stdout)    # 요약 줄


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 실패 확인** — `python3 -m pytest tests/test_wiki_lint.py -v` → FAIL (`ModuleNotFoundError`).

- [ ] **Step 3: 구현**

```python
# references/wiki/lint_wiki.py
"""omd wiki lint (G3) — report-only auditor for a .omd/wiki/ store. stdlib only.

ADAPTED from the omx wiki linter (omx-core wiki/lint.py, W5 "never auto-fix"):
omd wiki notes are free-form .md WITHOUT a machine schema (references/wiki/README.md),
so frontmatter is never required — only the optional `confidence:` marker is
inspected, staleness keys on file mtime, and the orphan check fires only when the
store actually uses [[wikilinks]] (a link-less store would false-flag every note).
Near-duplicate = filename-token Jaccard >= 0.5 (omx-measured threshold, borrowed).
NEVER gates: exit code is always 0 — docs-learn reads the report, a human decides.
"""
import argparse
import datetime
import os
import re
import sys

CATEGORIES = ("convention", "pattern", "decision", "reference")
WIKILINK = re.compile(r"\[\[([^\]|#]+)")
NEAR_DUP_JACCARD = 0.5
_STOP = frozenset({"the", "a", "an", "and", "or", "of", "to", "is", "in", "on",
                   "md", "spec", "notes", "note"})


def _tokens(name):
    raw = name[:-3] if name.endswith(".md") else name
    return {t for t in re.split(r"[-_\s]+", raw.lower()) if t and t not in _STOP}


def _mtime_dt(path):
    return datetime.datetime.fromtimestamp(os.path.getmtime(path))


def scan(root, now=None, stale_days=90, max_bytes=10240):
    """Audit the store. Returns [(severity, type, relpath, message)]. Never raises
    on content problems — a broken page is an issue, not a crash."""
    issues = []
    if not os.path.isdir(root):
        return [("info", "empty", root,
                 "no wiki store at this root (fresh project — not an error)")]
    now_dt = (datetime.datetime.fromisoformat(now) if now
              else datetime.datetime.now())

    pages = {}  # relpath -> (category, stem, body)
    for entry in sorted(os.listdir(root)):
        full = os.path.join(root, entry)
        if os.path.isfile(full) and entry.endswith(".md"):
            issues.append(("warning", "misplaced", entry,
                           f"expected under one of {CATEGORIES}"))
        elif os.path.isdir(full) and entry not in CATEGORIES:
            issues.append(("warning", "unknown-category", entry,
                           f"not one of {CATEGORIES}"))
        elif os.path.isdir(full):
            for name in sorted(os.listdir(full)):
                if not name.endswith(".md"):
                    continue
                rel = os.path.join(entry, name)
                try:
                    with open(os.path.join(full, name), encoding="utf-8",
                              errors="replace") as f:
                        body = f.read()
                except OSError as exc:
                    issues.append(("error", "unreadable", rel, str(exc)))
                    continue
                pages[rel] = (entry, name[:-3], body)

    # per-page checks
    stems = {stem for _, stem, _ in pages.values()}
    link_culture = any(WIKILINK.search(body) for _, _, body in pages.values())
    inbound = {stem: 0 for stem in stems}
    for rel, (cat, stem, body) in pages.items():
        path = os.path.join(root, rel)
        age = (now_dt - _mtime_dt(path)).days
        if age > stale_days:
            issues.append(("info", "stale", rel,
                           f"not touched in {age} days (> {stale_days})"))
        size = os.path.getsize(path)
        if size > max_bytes:
            issues.append(("warning", "oversized", rel,
                           f"{size} bytes > {max_bytes}"))
        if cat == "convention" and not re.search(r"^confidence:", body, re.M):
            issues.append(("info", "missing-confidence", rel,
                           "convention note without a confidence: marker"))
        for target in WIKILINK.findall(body):
            target = target.strip()
            if target in stems:
                inbound[target] += 1
            else:
                issues.append(("warning", "broken-ref", rel,
                               f"link target {target!r} does not exist"))

    # orphan — only when the store actually links (PD-2)
    if link_culture:
        for rel, (cat, stem, body) in sorted(pages.items()):
            path = os.path.join(root, rel)
            fresh = (now_dt - _mtime_dt(path)).days <= stale_days // 2
            if inbound.get(stem, 0) == 0 and not fresh:
                issues.append(("info", "orphan", rel, "no page links to this page"))

    # near-duplicate within a category (slug-fork failure mode)
    by_cat = {}
    for rel, (cat, stem, _) in pages.items():
        by_cat.setdefault(cat, []).append((rel, _tokens(stem)))
    for cat, items in sorted(by_cat.items()):
        items.sort()
        for i in range(len(items)):
            rel_a, tok_a = items[i]
            if not tok_a:
                continue
            for j in range(i + 1, len(items)):
                rel_b, tok_b = items[j]
                union = tok_a | tok_b
                if union and len(tok_a & tok_b) / len(union) >= NEAR_DUP_JACCARD:
                    issues.append(("info", "near-duplicate", rel_a,
                                   f"filename overlaps {rel_b!r} — read both; "
                                   f"if one topic, merge by hand"))
    return issues


def main(argv=None):
    ap = argparse.ArgumentParser(description="omd wiki lint — report-only, exit 0")
    ap.add_argument("--root", default=os.path.join(".omd", "wiki"))
    ap.add_argument("--now", default=None, help="ISO timestamp (tests inject this)")
    ap.add_argument("--stale-days", type=int, default=90)
    ap.add_argument("--max-bytes", type=int, default=10240)
    args = ap.parse_args(argv)
    issues = scan(args.root, now=args.now, stale_days=args.stale_days,
                  max_bytes=args.max_bytes)
    for sev, typ, rel, msg in issues:
        print(f"{sev:7s} {typ:18s} {rel} — {msg}")
    print(f"total: {len(issues)} issue(s) — report-only; nothing was modified")
    return 0  # ALWAYS — lint never gates (W5)


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 통과 확인** — `python3 -m pytest tests/test_wiki_lint.py -v` → PASS, 전체 suite green.
- [ ] **Step 5: Commit** — `git commit -am "feat(omd): wiki lint — report-only store auditor (G3, adapted from omx)"`

---

### Task 5: docs-learn 배선 — lint를 Step 0으로 (G3 소비자)

**Files:**
- Modify: `skills/docs-learn/SKILL.md`
- Modify: `tests/test_skill_contract.py` (pin 1개)

**Interfaces:**
- Consumes: Task 4의 `lint_wiki.py` CLI.
- Produces: docs-learn Steps에 wiki-health 리포트 단계.

- [ ] **Step 1: 실패하는 pin 테스트 추가**

```python
# tests/test_skill_contract.py 에 추가 (기존 스킬 본문 로드 헬퍼를 재사용할 것)
def test_docs_learn_calls_wiki_lint():
    """G3: docs-learn's steps run the report-only linter before promotion review."""
    body = open(os.path.join(SKILLS, "docs-learn", "SKILL.md"), encoding="utf-8").read()
    assert "lint_wiki.py" in body
    assert "report-only" in body
```

(기존 `tests/test_skill_contract.py`의 경로 상수(`SKILLS` 등)와 로드 방식을 먼저 읽고 동일 패턴으로 작성.)

- [ ] **Step 2: 실패 확인** — `python3 -m pytest tests/test_skill_contract.py -v` → 신규 1개 FAIL.

- [ ] **Step 3: SKILL.md 수정** — `## Steps`의 1번 앞에 Step 0 삽입(기존 번호는 유지하고 "0."으로 추가):

```markdown
0. **wiki health report (G3, report-only)**: run the mechanical linter over both wiki levels —
   `python3 <plugin>/references/wiki/lint_wiki.py --root .omd/wiki` (and, if an ancestor `.omd/`
   exists, once more with `--root <ancestor>/.omd/wiki`). Surface the report to the user as-is
   (stale / oversized / broken-ref / near-duplicate / missing-confidence). The linter never
   modifies anything and always exits 0 — findings are review input for this gate, not a block.
   python3 unavailable → skip with one line (degrade, fail-open).
```

- [ ] **Step 4: 통과 확인** — 전체 suite green (skill budget 테스트 포함).
- [ ] **Step 5: Commit** — `git commit -am "feat(omd): docs-learn step 0 — wiki lint health report wiring (G3)"`

---

### Task 6: notepad 3-tier + precompact_reinject 훅 (G2)

**Files:**
- Create: `hooks/precompact_reinject.py`
- Create: `references/notepad.md` (3-tier 계약 카드)
- Modify: `.claude-plugin/plugin.json` (PreCompact + SessionStart 등록)
- Modify: `skills/docs-pilot/SKILL.md` (Execution_Policy의 notepad 항목에 카드 포인터 1줄)
- Test: `tests/test_precompact_reinject.py`

**Interfaces:**
- Produces: `.omd/notepad.md` 3-tier 계약 (`## Priority Context` / `## Working Notes` / `## Manual`), 훅 동작 2종 — PreCompact=pruning(원자쓰기), SessionStart(compact)=Priority 재주입(additionalContext).
- Consumes: docs-pilot이 이미 쓰는 `## Priority Context` 섹션 (SKILL.md Execution_Policy).

**설계 (PD-1):** 한 파일이 `hook_event_name`으로 분기. PreCompact는 컨텍스트 주입 채널이 아니므로 재주입은 SessionStart(matcher `compact`)가 담당 — compaction 직후 새 컨텍스트가 시작될 때 Priority Context를 되살린다. pruning은 PreCompact 시점: notepad가 MAX_BYTES(16KiB) 초과 시 `## Working Notes`만 마지막 KEEP_LINES(40줄)로 절단(ST-1 원자쓰기), `## Priority Context`·`## Manual`은 절대 불변.

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_precompact_reinject.py
"""G2 — hooks/precompact_reinject.py: notepad pruning + compaction-survival reinject."""
import json
import os
import subprocess
import sys
import tempfile
import unittest

HOOK = os.path.join(os.path.dirname(__file__), "..", "hooks", "precompact_reinject.py")
PLUGIN_JSON = os.path.join(os.path.dirname(__file__), "..", ".claude-plugin", "plugin.json")

NOTEPAD = """# omd notepad

## Priority Context
- no in-place modification of the original
- final is a single outputs/deck/current.pptx; gate 2 passed

## Working Notes
{working}

## Manual
- user's own line — never touched
"""


def run_hook(payload):
    return subprocess.run([sys.executable, HOOK], input=json.dumps(payload),
                          capture_output=True, text=True)


def write_notepad(cwd, working_lines):
    os.makedirs(os.path.join(cwd, ".omd"), exist_ok=True)
    body = NOTEPAD.format(working="\n".join(working_lines))
    with open(os.path.join(cwd, ".omd", "notepad.md"), "w", encoding="utf-8") as f:
        f.write(body)
    return os.path.join(cwd, ".omd", "notepad.md")


class TestReinject(unittest.TestCase):
    def test_session_start_compact_reinjects_priority(self):
        with tempfile.TemporaryDirectory() as tmp:
            write_notepad(tmp, ["- note"])
            out = run_hook({"hook_event_name": "SessionStart", "source": "compact",
                            "cwd": tmp})
            self.assertEqual(out.returncode, 0)
            payload = json.loads(out.stdout)
            ctx = payload["hookSpecificOutput"]["additionalContext"]
            self.assertIn("no in-place modification", ctx)
            self.assertNotIn("user's own line", ctx)  # Priority만 재주입

    def test_no_notepad_is_silent(self):
        with tempfile.TemporaryDirectory() as tmp:
            out = run_hook({"hook_event_name": "SessionStart", "source": "compact",
                            "cwd": tmp})
            self.assertEqual(out.returncode, 0)
            self.assertEqual(out.stdout.strip(), "")

    def test_broken_payload_fails_open(self):
        out = subprocess.run([sys.executable, HOOK], input="not json{",
                             capture_output=True, text=True)
        self.assertEqual(out.returncode, 0)


class TestPrune(unittest.TestCase):
    def test_oversized_working_notes_trimmed_atomically(self):
        with tempfile.TemporaryDirectory() as tmp:
            lines = [f"- working item {i} " + "x" * 300 for i in range(60)]  # ≈19KiB > 16KiB
            path = write_notepad(tmp, lines)
            out = run_hook({"hook_event_name": "PreCompact", "trigger": "auto",
                            "cwd": tmp})
            self.assertEqual(out.returncode, 0)
            body = open(path, encoding="utf-8").read()
            self.assertIn("no in-place modification", body)   # Priority 불변
            self.assertIn("user's own line", body)            # Manual 불변
            self.assertIn("working item 59", body)            # 최신 유지
            self.assertNotIn("working item 0", body)          # 오래된 것 절단
            self.assertLess(len(body.encode("utf-8")), 17000)

    def test_small_notepad_untouched(self):
        with tempfile.TemporaryDirectory() as tmp:
            path = write_notepad(tmp, ["- one line"])
            before = open(path, encoding="utf-8").read()
            run_hook({"hook_event_name": "PreCompact", "trigger": "manual", "cwd": tmp})
            self.assertEqual(open(path, encoding="utf-8").read(), before)


class TestRegistration(unittest.TestCase):
    def test_plugin_json_registers_both_events(self):
        cfg = json.load(open(PLUGIN_JSON, encoding="utf-8"))
        hooks = cfg["hooks"]
        flat = json.dumps(hooks.get("PreCompact", [])) + json.dumps(hooks.get("SessionStart", []))
        self.assertEqual(flat.count("precompact_reinject.py"), 2)
        # SessionStart는 compact 매처만 — startup 마다 발화하지 않게
        self.assertEqual(hooks["SessionStart"][0]["matcher"], "compact")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 실패 확인** — `python3 -m pytest tests/test_precompact_reinject.py -v` → FAIL (훅 파일 없음 → returncode ≠ 0 / FileNotFoundError).

- [ ] **Step 3: 훅 구현**

```python
# hooks/precompact_reinject.py
"""OMD notepad compaction-survival hook (G2). stdlib only, fail-open.

One file, two registrations (hook_event_name branches):
- PreCompact          : PRUNE — if .omd/notepad.md exceeds MAX_BYTES, trim the
  '## Working Notes' section to its newest KEEP_LINES lines (atomic os.replace,
  ST-1). '## Priority Context' and '## Manual' are NEVER touched. Emits nothing.
- SessionStart(compact): REINJECT — emit the '## Priority Context' section as
  additionalContext so pipeline-critical constraints (original-destruction ban,
  current gate, slug) survive the compaction boundary. docs-pilot writes that
  section on entry (skills/docs-pilot Execution_Policy); contract card:
  references/notepad.md.
Advisory infrastructure only — a broken notepad must never block anything.
"""
import json
import os
import sys
import tempfile

PRIORITY = "## Priority Context"
WORKING = "## Working Notes"
MANUAL = "## Manual"
MAX_BYTES = 16 * 1024
KEEP_LINES = 40


def _split_sections(text):
    """[(heading_or_None, [lines])] — preserves order and non-section preamble."""
    sections, current = [], (None, [])
    for line in text.splitlines():
        if line.startswith("## "):
            sections.append(current)
            current = (line.strip(), [])
        else:
            current[1].append(line)
    sections.append(current)
    return sections


def _section_body(text, heading):
    for head, lines in _split_sections(text):
        if head == heading:
            return "\n".join(lines).strip()
    return ""


def reinject(notepad_path):
    if not os.path.isfile(notepad_path):
        return
    with open(notepad_path, encoding="utf-8", errors="replace") as f:
        priority = _section_body(f.read(), PRIORITY)
    if priority:
        print(json.dumps({
            "hookSpecificOutput": {
                "hookEventName": "SessionStart",
                "additionalContext": (
                    "[OMD notepad] compaction 이전의 Priority Context 재주입:\n"
                    + priority),
            },
        }))


def prune(notepad_path):
    if not os.path.isfile(notepad_path):
        return
    if os.path.getsize(notepad_path) <= MAX_BYTES:
        return
    with open(notepad_path, encoding="utf-8", errors="replace") as f:
        text = f.read()
    out = []
    for head, lines in _split_sections(text):
        if head == WORKING and len(lines) > KEEP_LINES:
            kept = lines[-KEEP_LINES:]
            lines = ["<!-- pruned by precompact_reinject (G2): oldest working "
                     "notes trimmed -->"] + kept
        if head is not None:
            out.append(head)
        out.extend(lines)
    fd, tmp = tempfile.mkstemp(dir=os.path.dirname(notepad_path), suffix=".tmp")
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            f.write("\n".join(out) + "\n")
        os.replace(tmp, notepad_path)  # ST-1: atomic, never open+truncate
    except Exception:
        try:
            os.unlink(tmp)
        except OSError:
            pass
        raise


def main():
    try:
        payload = json.load(sys.stdin)
        notepad = os.path.join(payload.get("cwd") or os.getcwd(), ".omd", "notepad.md")
        event = payload.get("hook_event_name", "")
        if event == "SessionStart":
            reinject(notepad)
        elif event == "PreCompact":
            prune(notepad)
    except Exception:
        pass  # fail-open — advisory infrastructure must never block
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: plugin.json 등록** — `"hooks"` 오브젝트에 추가 (기존 키들 뒤):

```json
"PreCompact": [
  {
    "matcher": "*",
    "hooks": [
      { "type": "command", "command": "python3",
        "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/precompact_reinject.py"] }
    ]
  }
],
"SessionStart": [
  {
    "matcher": "compact",
    "hooks": [
      { "type": "command", "command": "python3",
        "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/precompact_reinject.py"] }
    ]
  }
]
```

- [ ] **Step 5: notepad 계약 카드 작성**

```markdown
# references/notepad.md — `.omd/notepad.md` 3-tier contract (G2)

`.omd/notepad.md` is the work-root scratch memory that survives compaction. Three
fixed sections (exact headings — the hook greps them):

| Section | Writer | Pruning | Purpose |
|:--|:--|:--|:--|
| `## Priority Context` | docs-pilot on entry (pipeline-critical constraints: original-destruction ban, single `current.<ext>` invariant, current gate + slug) | **never pruned** | reinjected verbatim after compaction (SessionStart source=compact → additionalContext) |
| `## Working Notes` | any stage, freely | hook trims to the newest 40 lines when the file exceeds 16KiB (PreCompact, atomic ST-1) | session-scoped scratch |
| `## Manual` | the user only | **never touched by any tool** | user-owned notes |

- .md is the default and sufficient; a notepad MCP is an optional mirror, never required
  (docs-pilot Execution_Policy).
- The hook (`hooks/precompact_reinject.py`) is advisory infrastructure: fail-open, never
  blocks, never rewrites Priority/Manual, and prunes only when oversized.
- This file is the omd adaptation of OMC's notepad 3-tier (Priority/Working/Manual);
  the .md-degrade philosophy comes from `references/omc-backport-analysis.md` T11.
```

- [ ] **Step 6: docs-pilot 포인터 1줄** — Execution_Policy의 notepad 항목(".md is the default" 불릿) 끝에 추가: `` Contract (3 tiers, pruning, compaction-survival): `references/notepad.md`. ``

- [ ] **Step 7: 통과 확인** — `python3 -m pytest tests/test_precompact_reinject.py -v` → PASS, 전체 suite green (plugin integrity·distribution axiom 포함).
- [ ] **Step 8: Commit** — `git add -A && git commit -m "feat(omd): notepad 3-tier + compaction survival hook (G2) — prune on PreCompact, reinject on SessionStart(compact)"`

---

### Task 7: stage-evidence 마커 + stop guard 확장 (G5)

**Files:**
- Modify: `skills/docs-pilot/SKILL.md` (Execution_Policy에 마커 규칙 1불릿)
- Modify: `hooks/docs_stop_guard.py` (누락 grep)
- Modify: `tests/test_stop_guard.py` (케이스 3개)

**Interfaces:**
- Produces: 마커 계약 — docs-pilot이 각 스테이지 dispatch 시 `.omd/<slug>/stage-evidence.log`에 한 줄 append: `OMD stage <n> <name> → spawned oh-my-docs:doc-<agent>` 또는 `OMD stage <n> <name> → skipped: <reason>` (n=1..6: intake·standardize·plan·build·inspect·verify). stop guard는 mtime 6h 이내 로그에서 1..6 누락을 advisory로 보고.
- Consumes: `docs_stop_guard.py`의 기존 골격 (`STALE_AFTER`, fail-open, stop_hook_active 가드, additionalContext 조립).

- [ ] **Step 1: 실패하는 테스트 추가** — 기존 `tests/test_stop_guard.py`의 `run_hook` 하니스(문자열 시그니처 주의 — R3 실측: payload는 json 문자열로 넘긴다)를 먼저 읽고 그대로 재사용:

```python
# tests/test_stop_guard.py 에 추가
def _write_evidence(cwd, slug, lines, old=False):
    d = os.path.join(cwd, ".omd", slug)
    os.makedirs(d, exist_ok=True)
    p = os.path.join(d, "stage-evidence.log")
    with open(p, "w", encoding="utf-8") as f:
        f.write("\n".join(lines) + "\n")
    if old:
        past = time.time() - 8 * 3600
        os.utime(p, (past, past))
    return p


def test_stage_evidence_gap_reported(tmp_path):
    """G5: a recent pilot log missing stage markers -> advisory names the gaps."""
    _write_evidence(str(tmp_path), "deck", [
        "OMD stage 1 intake → spawned oh-my-docs:doc-analyzer",
        "OMD stage 2 standardize → skipped: no reference documents",
        "OMD stage 4 build → spawned oh-my-docs:doc-builder",
    ])
    out = run_hook({"cwd": str(tmp_path)})
    assert "stage-evidence" in out
    for missing in ("3", "5", "6"):
        assert missing in out


def test_stage_evidence_complete_is_silent(tmp_path):
    _write_evidence(str(tmp_path), "deck", [
        f"OMD stage {n} x → spawned oh-my-docs:doc-x" for n in range(1, 7)])
    out = run_hook({"cwd": str(tmp_path)})
    assert "stage-evidence" not in out


def test_stage_evidence_old_log_ignored(tmp_path):
    _write_evidence(str(tmp_path), "deck", ["OMD stage 1 intake → spawned x"], old=True)
    out = run_hook({"cwd": str(tmp_path)})
    assert "stage-evidence" not in out
```

(⚠️ 기존 파일의 하니스 이름·반환 형태(stdout 문자열인지 dict인지)를 먼저 확인하고 assert를 그 형태에 맞출 것. 위는 "stdout 전체 문자열" 가정.)

- [ ] **Step 2: 실패 확인** — `python3 -m pytest tests/test_stop_guard.py -v` → 신규 3개 FAIL.

- [ ] **Step 3: 훅 확장** — `docs_stop_guard.py`에 추가:

```python
import re  # 상단 import 블록에

EVIDENCE_LOG = "stage-evidence.log"
PILOT_STAGES = ("1", "2", "3", "4", "5", "6")  # intake..verify (docs-pilot Steps 1-6)


def stage_evidence_gaps(root):
    """G5: recent pilot runs (log mtime within STALE_AFTER) missing stage markers."""
    gaps = []
    if not os.path.isdir(root):
        return gaps
    now = time.time()
    for d in sorted(os.listdir(root)):
        log = os.path.join(root, d, EVIDENCE_LOG)
        try:
            if not os.path.isfile(log) or now - os.path.getmtime(log) > STALE_AFTER:
                continue
            body = open(log, encoding="utf-8", errors="replace").read()
        except OSError:
            continue
        seen = set(re.findall(r"OMD stage (\d)", body))
        missing = [n for n in PILOT_STAGES if n not in seen]
        if missing:
            gaps.append((d, missing))
    return gaps
```

`main()`에서 `items = pending_sentinels(root)` 다음에 `gaps = stage_evidence_gaps(root)`를 계산하고, 메시지 조립을 두 블록 합성으로 확장(어느 한쪽만 있어도 발화, 둘 다 없으면 침묵). G5 블록 문구:

```python
def build_evidence_message(gaps):
    lines = [f"  - {slug}: stage {', '.join(missing)}" for slug, missing in gaps]
    return (
        "[OMD stage-evidence] 최근 pilot 실행에서 spawned/skipped 마커가 없는 스테이지:\n"
        + "\n".join(lines)
        + "\n마커가 없다는 것은 해당 스테이지가 위임 없이 인라인 처리됐을 수 있다는 뜻입니다"
        " (author≠reviewer 분리 약화). advisory입니다 — 의도적 인라인이었다면 무시하세요."
    )
```

(기존 `build_message`는 그대로 두고, `main()`에서 두 메시지를 `"\n\n"`으로 join — 둘 중 있는 것만.)

- [ ] **Step 4: docs-pilot 마커 규칙** — Execution_Policy에 불릿 추가:

```markdown
- **Stage-evidence markers (G5)**: at every stage 1–6 decision, append exactly one line to
  `.omd/<slug>/stage-evidence.log` — `OMD stage <n> <name> → spawned oh-my-docs:doc-<agent>`
  when dispatching, or `OMD stage <n> <name> → skipped: <reason>` when the skip condition
  holds. The Stop guard greps this log and flags stages with neither marker (advisory) —
  the delegation-evidence trail is how "the pilot quietly did a stage inline" gets caught.
```

- [ ] **Step 5: 통과 확인** — `python3 -m pytest tests/test_stop_guard.py -v` → PASS, 전체 suite green.
- [ ] **Step 6: Commit** — `git commit -am "feat(omd): stage-evidence markers + stop-guard gap grep (G5)"`

---

### Task 8: ownership guard — manifest 대조 삭제/덮어쓰기 안전장치 (G6)

**Files:**
- Modify: `references/output-layout.md` (§3.4 신설 + §6 체크리스트 1항)
- Modify: `skills/docs-build/SKILL.md`, `skills/docs-revise/SKILL.md`, `agents/doc-builder.md` (각 1줄)
- Modify: `skills/docs-pilot/SKILL.md` (Step 8 cleanup에 1줄)
- Modify: `tests/test_output_layout_contract.py` (pin 2개)

**Interfaces:**
- Consumes: `.omd/<slug>/manifest.json` 스키마 (output-layout §3.3, R2 산출) — `{"slug","format","paths":[{"path","sha256","role"}]}`.
- Produces: §3.4 가드 규칙 — 이후 카드/스킬이 참조하는 SSOT.

- [ ] **Step 1: 실패하는 pin 테스트 추가**

```python
# tests/test_output_layout_contract.py 에 추가 (기존 로드 패턴 재사용)
def test_ownership_guard_section_present():
    """G6: overwrite/delete under an artifact-set current/ must consult the manifest."""
    text = open(LAYOUT, encoding="utf-8").read()
    assert "### 3.4 Ownership guard (G6)" in text
    assert "AskUserQuestion" in text.split("### 3.4")[1].split("## 4.")[0]

def test_ownership_guard_consumers_wired():
    for rel in ("skills/docs-build/SKILL.md", "skills/docs-revise/SKILL.md",
                "agents/doc-builder.md"):
        body = open(os.path.join(ROOT, rel), encoding="utf-8").read()
        assert "3.4" in body and "manifest" in body.lower(), rel
```

- [ ] **Step 2: 실패 확인** — `python3 -m pytest tests/test_output_layout_contract.py -v` → 신규 FAIL.

- [ ] **Step 3: output-layout §3.4 신설** — §3.3 바로 아래 삽입:

```markdown
### 3.4 Ownership guard (G6)

Before **overwriting or deleting anything under `outputs/<slug>/current/`** (artifact-set
rebuilds, revise loops, any cleanup that would touch it), consult `.omd/<slug>/manifest.json`:

1. **Not listed in `paths[]`** → omd did not write it (a user file may have landed inside
   `current/`). Never touch it silently — AskUserQuestion (overwrite/delete vs keep).
2. **Listed but sha256 differs** from the manifest value (`shasum -a 256 <file>` on macOS,
   `sha256sum` on Linux) → the user hand-edited it since the last build. Never clobber
   silently — AskUserQuestion (their edit would be lost).
3. **Manifest absent or unparseable** → treated as "no ownership evidence": same ask path.
   (ST-1 atomic writes exist precisely so a half-written manifest reads as absent, §3.3.)

Single-file genres (`current.<ext>`) keep their existing protection — the version snapshot
(§3.1) before large edits. The guard is advisory-by-human-gate, not a hook: the asking is
done by the skill/agent about to write (docs-build, docs-revise, doc-builder, pilot cleanup).
`outputs/` deletion remains forbidden for cleanup regardless (§5.2 — this guard covers
*rebuild overwrites*, which are legitimate but must not eat user edits).
```

§6 체크리스트에 추가: `- [ ] artifact-set overwrite/delete consults manifest.json first (§3.4) — unlisted or hash-drifted files go through AskUserQuestion`

- [ ] **Step 4: 소비자 1줄씩** — 각 파일의 빌드/수정 절차 서술부에:
  - `skills/docs-build/SKILL.md` (artifact-set 빌드 언급부 근처): `Artifact-set rebuild: before overwriting files under \`current/\`, run the ownership guard — \`references/output-layout.md\` §3.4 (manifest check; unlisted/hash-drifted → AskUserQuestion).`
  - `skills/docs-revise/SKILL.md` (수정 루프 서술부): 동일 취지 1줄.
  - `agents/doc-builder.md` (산출물 쓰기 계약부): 동일 취지 1줄.
  - `skills/docs-pilot/SKILL.md` Step 8 tally 불릿 끝: `Anything inside \`outputs/<slug>/current/\` is never a cleanup target; rebuild-time overwrites go through the ownership guard (output-layout §3.4).`

- [ ] **Step 5: 통과 확인** — 전체 suite green.
- [ ] **Step 6: Commit** — `git commit -am "feat(omd): ownership guard — manifest-checked overwrite/delete (G6)"`

---

### Task 9: capture 자동화 — OBS 초안 생성 경로 (spec §4.5c)

**Files:**
- Modify: `skills/docs-pilot/SKILL.md` (Step 7b 신설)
- Modify: `skills/docs-inspect/SKILL.md`, `skills/docs-verify/SKILL.md`, `skills/docs-standardize/SKILL.md` (capture 스텝 각 1개)
- Modify: `references/learning-protocol.md` (재관찰 semantics 1절)
- Modify: `tests/test_wiki_two_level.py` (pin 1개)

**Interfaces:**
- Consumes: `.omd/learned.md` OBS 블록 포맷 (learning-protocol §2 — 이미 존재), Task 2의 `title_to_slug`/`safe_wiki_path` (wiki 쓰기 시).
- Produces: capture 경로 — "원장이 채워질 수 없음" 문제의 해소 (§4.5의 마지막 조각).

**규율 (PD-5):** capture는 값싸다 — 세션이 실제로 관찰한 것만, 추측 0, 블록당 몇 줄. 승격 판단은 하지 않는다(그건 docs-learn+human gate). content(텍스트·주장·수치·출처)는 OBS 대상이 아니다(§6.F).

- [ ] **Step 1: 실패하는 pin 테스트 추가**

```python
# tests/test_wiki_two_level.py 에 추가
def test_capture_path_wired_in_stages():
    """§4.5c: the ledger can now actually be filled — pilot 7b + the three
    observing stages each carry an explicit OBS-capture step."""
    for rel in ("skills/docs-pilot/SKILL.md", "skills/docs-inspect/SKILL.md",
                "skills/docs-verify/SKILL.md", "skills/docs-standardize/SKILL.md"):
        body = open(os.path.join(ROOT, rel), encoding="utf-8").read()
        assert "learned.md" in body and "OBS" in body, rel
```

- [ ] **Step 2: 실패 확인** — 신규 pin FAIL (docs-pilot 외 스킬에 OBS 언급 없음).

- [ ] **Step 3: docs-pilot Step 7b** — Step 7 블록 끝에 추가:

```markdown
   - **7b. heavy-channel OBS capture (cheap, automatic, draft-only)**: if this session
     *actually observed* a repeatable FORM pattern — the same defect flagged again, the same
     style preference re-confirmed, or the user stating a rule out loud — append/refresh the
     matching `OBS-NNNN` block in `.omd/learned.md` (format: `learning-protocol.md` §2):
     a new pattern appends a new block (`status: candidate`, `source_stage`, one evidence
     line); a re-sighting of an existing block updates `evidence_count`/`last_seen` and adds
     one evidence line in place (§2 re-sighting semantics — never delete/retire, that is
     docs-learn's job). A user-stated rule sets `user_stated: true`. Capture is a *draft*:
     promotion still requires the docs-learn human gate. Never capture content
     (text/claims/numbers/sources — §6.F), never capture a guess, keep it to a few lines.
     Nothing observed → skip silently.
```

- [ ] **Step 4: 관찰 3스킬에 capture 스텝** — 각 SKILL.md의 Steps 마지막에 (문구는 스킬 어휘에 맞게 미세 조정 가능, 계약 요소는 유지 — learned.md·OBS·추측 금지·content 금지·human gate):

```markdown
N. **OBS capture (cheap, draft-only)**: if this run surfaced a repeatable FORM pattern
   (recurring defect, re-confirmed style preference, or a user-stated rule), append/refresh
   the matching `OBS-NNNN` block in `.omd/learned.md` per `learning-protocol.md` §2 —
   re-sighting updates `evidence_count`/`last_seen` in place; new pattern appends a new
   candidate block. Draft only (promotion = docs-learn human gate). Never content (§6.F),
   never a guess. Nothing observed → skip.
```

- [ ] **Step 5: learning-protocol 재관찰 semantics** — §2(OBS 스펙) 끝에 1절 추가 (기존 §2 본문을 먼저 읽고 중복이면 이 스텝은 문구 보강만):

```markdown
### §2.x Re-sighting update semantics (capture channel)

The ledger is append-only in the sense that **no block is ever deleted or retired by a
capturing stage** (only docs-learn retires, through the human gate). A *re-sighting* of an
already-ledgered pattern does NOT append a duplicate block — it updates the existing block
in place: `evidence_count += 1`, `last_seen` refreshed, one line added under `evidence:`.
Capture-then-curate split: capture (any observing stage, cheap, automatic, draft-only) fills
the ledger; curate (docs-learn, human gate) is the only path that changes a default.
```

- [ ] **Step 6: 통과 확인** — 전체 suite green (skill budget 상한 확인 포함).
- [ ] **Step 7: Commit** — `git commit -am "feat(omd): OBS capture path — pilot 7b + observing stages (spec 4.5c, capture-then-curate complete)"`

---

### Task 10: Release — v0.4.0

**Files:**
- Modify: `.claude-plugin/plugin.json` (version 0.3.0 → 0.4.0)
- Modify: `CHANGELOG.md` (0.4.0 엔트리 — 기존 파일의 형식·정책 헤더를 먼저 읽고 그 관례를 따를 것)
- Modify: `README.md` (Status 절에 R4 반영 — 기존 표기 스타일 유지)

- [ ] **Step 1: version bump** — plugin.json `"version": "0.4.0"`.
- [ ] **Step 2: CHANGELOG 0.4.0 엔트리** — Added: query_helper(KN-2·KN-3·KN-4)/lint_wiki(G3)/precompact_reinject+notepad 계약(G2)/stage-evidence(G5)/ownership guard(G6)/OBS capture 경로(§4.5c), Changed: wiki README 계약 3건(ST-3 포함)·docs-learn Step 0·docs-pilot 7b, Verification: `python3 -m pytest -q` 결과 수치 명기.
- [ ] **Step 3: README Status 갱신** — text-genre/지식 수명주기 완성 1-2줄, 버전 언급이 이미 있으면 0.4.0으로(신설 주입은 금지 — DT-2).
- [ ] **Step 4: 전체 검증** — `python3 -m pytest -q` → 전부 green (기대: 136 + 신규 ≈ 155±). `git log --oneline main..HEAD`로 태스크별 커밋 확인.
- [ ] **Step 5: Commit** — `git commit -am "feat(omd): release v0.4.0 — knowledge lifecycle (G2·G3·G5·G6, KN-2/3/4, ST-3, OBS capture)"`

---

## Self-Review 체크 (plan 작성자 수행 완료)

- **Spec coverage**: §5 R4행의 G2·G3·G5·G6·capture·KN-2·KN-3·ST-3·KN-4 전부 태스크 매핑(1~9) + Release(10). 흡수분 ST-1은 Task 6 원자쓰기에 반영. G1-chk 재진입 가드는 기존 코드 유지(Task 7이 손대지 않음).
- **Type consistency**: `tokenize/match/safe_wiki_path/title_to_slug` 시그니처가 Task 1·2·3·9에서 동일. `STALE_AFTER` 재사용(Task 7). manifest 스키마는 §3.3 기존 정의 참조.
- **Placeholder scan**: 통과 — 모든 코드 스텝에 실제 코드, 모든 계약 문구에 실제 문장.
