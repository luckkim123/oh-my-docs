# omd v0.3.0 R3 — site 장르 (MkDocs + Material) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** omd에 site 장르(MkDocs+Material 정적 문서 사이트)를 카드로 편입하고, 이 머신에서 엔진을 실측 검증(VERIFIED 스탬프)하며, omd 자체 문서 사이트 E2E 파일럿 1회 성공으로 릴리스한다 (spec §5 R3).

**Architecture:** spec §4.2가 SSOT. 새 포맷 = 새 카드(D1) — `references/formats/site.md` + `references/rubrics/site-rubric.md` 2파일이 지식의 전부이고, 스킬/에이전트/훅은 R2가 만든 카드-위임 인프라에 site 토큰·행·어휘만 추가한다. verify는 결정론적 exit-code 체인(D3): `mkdocs build --strict` + validation 블록이 1차 기계 게이트.

**Tech Stack:** Python stdlib(훅·테스트) / 빌린 엔진: MkDocs 1.6.1 + mkdocs-material (`uvx` 임시 실행), markdownlint-cli2 (`npx`), lychee(선택·미설치) / pytest.

**Base:** `feat/v0.3.0-r3` @ dbbe8aa (main, v0.2.0 머지 직후). 시작 suite: **115 passed**.

## Global Constraints

- **D6**: 모든 훅 신규 동작은 fail-open advisory — `decision:block` 금지, 예외 시 exit 0.
- **D8**: 배포 파일(skills/agents/hooks/references/.claude-plugin)에 개인 식별자·홈 절대경로 금지 — `test_distribution_axiom.py`가 스캔. 엔진 스탬프는 날짜+버전만(머신명·경로 금지).
- **D1**: 새 포맷 = 새 카드. 스킬/에이전트 본문에 포맷 if/else 신설 금지 — "카드가 정의한다"로 위임.
- **D3**: 필수 엔진 미설치 verdict = `UNVERIFIED (engine unavailable)` — 조용한 PASS도 FAIL도 금지.
- **D7**: FORM만 학습(nav 깊이 선호·palette scheme·quadrant 균형), CONTENT(페이지 사실·API 이름·링크 대상) 영구 금지.
- **ST-1**: manifest/센티널 쓰기는 tmp 파일 + `os.replace()` 원자 교체.
- **하위 호환**: 오피스 4포맷 + repo-docs의 파이프라인·카드·verify 경로 무변경. 기존 115 테스트는 반전 1건(Task 4의 site pin)외 무수정 green.
- **매 태스크 종료 시** `python3 -m pytest tests/ -q` green + 커밋. 훅·스크립트는 stdlib-only.
- **엔진은 훅이 아니라 doc-verifier의 Bash 경유로만 실행** — 훅에 외부 의존 유입 금지.

---

## Plan-level decisions (spec이 plan 층에 위임한 결정 — critic 검토 대상)

1. **엔진 실행 경로 = 임시 실행 러너**: 이 머신 실측(2026-07-13) — system python3 3.9.6은 부적합, `uvx --from mkdocs --with mkdocs-material mkdocs` 가 mkdocs 1.6.1(uv 관리 Python 3.11)로 즉시 구동됨. markdownlint는 R2 선례대로 `npx markdownlint-cli2`. 카드 Engine 표에 **러너 형태까지** 검증 경로로 기록(전역 설치 불요·비권장 명시). lychee는 미설치 유지(optional, UNVERIFIED).
2. **mkdocs 신호의 build/verify 이중성 해소 — verify-우선 매칭**: `mkdocs build --strict`(카드 gate ①)는 verify 실행이고, plain `mkdocs build`는 산출 빌드다. `docs_verify_emit.py`에 `is_verify_run()` 신설 — VERIFY_SIGNALS 또는 (`"mkdocs"` ∧ `"--strict"`) 토큰 쌍이면 **build 판정보다 먼저** 센티널 clear 후 종료. 기존 VERIFY_SIGNALS 3종은 BUILD_SIGNALS와 교집합이 없어 오피스 동작은 무변(회귀 테스트로 고정). `"mkdocs build"`는 BUILD_SIGNALS에 추가.
3. **E2E 파일럿의 커밋 위치**: `.gitignore`가 `outputs/*`·`.omd/`를 비커밋으로 고정하므로(정책 유지), 파일럿은 ① `outputs/omd-site/current/`에서 저작·검증(실제 파이프라인 경로 — D5 훅 발화·manifest·verify-runs 증거 확보) ② 통과한 **소스 세트만** `tests/fixtures/omd-site/current/`로 스냅샷 커밋 — 영구 가드(Task 9)의 대상. **이것은 신규 메커니즘이다** (ultracode 검증 정정: R2 dogfood는 repo의 살아있는 README/CHANGELOG를 직접 읽는 방식이고 fixture 복사 선례는 repo에 없음) — 트레이드오프는 "살아있는 dogfood가 아닌 동결 스냅샷"(fixture는 저절로 낡지 않는 대신 저절로 최신화되지도 않음). omd repo에 상시 게시할 사이트가 없는 상황에서 outputs/ 비커밋 정책을 지키는 최소 대안으로 선택 — PR 비준 항목에 이 트레이드오프를 그대로 기재해 사용자가 실제 설계를 승인하게 한다.
4. **built HTML의 자리 = `.omd/<slug>/site-build/`**: mkdocs 기본 출력(`site/`)이 `current/` 안에 떨어지면 D4 불변식("진입점은 current 하나 = 소스 세트")이 깨진다. 빌드는 항상 `-d`로 작업영역에 우회 — 오피스의 `renders/current/`(파생 증거)와 동형. 카드 Hard trap으로 명문화.
5. **gate ⑤(nav 완결성)의 구현 위치**: 카드 표준 mkdocs.yml의 `validation:` 블록(`nav.omitted_files: warn` 포함) + `--strict`가 1차 커버(spec §4.2 "‑‑strict가 커버하는지 실측 확인 후 중복 제거"의 이행 — Task 3 실측이 확정). stdlib 이중 체크는 항상-켜진 영구 가드(Task 9)에만 둔다 — 엔진 없이도 fixture 회귀를 잡기 위함이지 gate 중복이 아님. 실측이 예상과 다르면 카드에 실측 결과를 기록하고 gate ⑤를 독립 체크로 승격(Task 3 Step 4 분기).
6. **MQ-2 재판정 = 재기각**: R2가 R3 재검토로 이연한 "verify 산출물 fenced JSON 격상"은 재기각 — 소비자 부재가 지속되고(docs-revise는 표를 직접 소비, site 장르도 새 소비자를 만들지 않음) verify-runs/ 로그가 이미 기계 증거 채널이다. PR 본문의 사용자 비준 항목으로 명시(R2의 정책 비준 패턴).
7. **site 카드 계약**: `GENRE_CARDS`에 `site.md` 편입(필수 3섹션 공통 계약) + site 전용 완결성 토큰 테스트. License_Note는 조건 섹션(FV-1) — site는 표준 텍스트 복사가 없으므로 Sources만 둔다(Diátaxis·MkDocs·Material 공식 문서 인용).
8. **R2 이월 코스메틱 소진**: plugin.json description·README 3곳(Formats/Status/Architecture)·doc-planner 잔재 2곳(Final_Checklist "narrative arc" 고정·Deliberate 트리거의 발표 한정 어휘)을 Task 10에서 처리. docs-plan·docs-standardize·doc-builder·output-layout은 R2가 이미 site를 명명했으므로 무수정.

---

## File map (전체 산출·수정 대상)

| Task | Create | Modify |
|:--|:---|:---|
| 1 | `references/formats/site.md` | `tests/test_format_cards.py` |
| 2 | `references/rubrics/site-rubric.md` | `tests/test_genre_generalization.py` |
| 3 | (실측 로그는 비커밋) | `references/formats/site.md`, `tests/test_format_cards.py` |
| 4 | — | `hooks/route_emit.py`, `tests/test_route_emit.py` |
| 5 | — | `hooks/docs_verify_emit.py`, `tests/test_verify_emit.py` |
| 6 | — | `skills/docs-intake/SKILL.md`, `agents/doc-analyzer.md`, `tests/test_genre_generalization.py` |
| 7 | — | `skills/docs-build/SKILL.md`, `tests/test_genre_generalization.py` |
| 8 | `tests/fixtures/omd-site/current/**` (mkdocs.yml + docs/ 6페이지) | (outputs/·.omd/ 는 비커밋 작업) |
| 9 | `tests/test_site_dogfood.py` | — |
| 10 | — | `.claude-plugin/plugin.json`(description), `README.md`, `agents/doc-planner.md`, `tests/test_genre_generalization.py` |
| 11 | — | `.claude-plugin/plugin.json`(version), `CHANGELOG.md` |

---

### Task 1: site 포맷 카드 신설 (UNVERIFIED 출발) + 카드 계약 테스트

**Files:**
- Create: `references/formats/site.md`
- Modify: `tests/test_format_cards.py` (GENRE_CARDS 확장 + site 완결성 테스트)

**Interfaces:**
- Produces: `references/formats/site.md` — 이후 태스크 전부가 참조하는 SSOT. verify gate ①~⑤ 정의(①`mkdocs build --strict` ②markdownlint ③내부 링크·앵커 ④built HTML fresh-read ⑤nav 완결성), 표준 mkdocs.yml 스켈레톤(validation 블록 필수), built HTML 우회 규칙(`.omd/<slug>/site-build/`).

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_format_cards.py`의 `GENRE_CARDS` 줄을 수정하고 site 테스트 2개를 파일 끝에 추가:

기존:
```python
GENRE_CARDS = ["repo-docs.md"]
```
수정:
```python
GENRE_CARDS = ["repo-docs.md", "site.md"]
```
파일 끝에 추가:
```python
# ── R3: site 장르 카드 계약 ──────────────────────────────────────────
def test_site_gate_is_deterministic_and_complete():
    text = (FORMATS_DIR / "site.md").read_text(encoding="utf-8")
    for token in (
        "## Verify gate",
        "mkdocs build --strict",              # gate ① — 1차 기계 게이트
        "Diátaxis",                           # 구조 프레임 (doc-planner 소비)
        "validation",                         # 표준 mkdocs.yml validation 블록
        "omitted_files",                      # gate ⑤ nav 완결성의 1차 커버
        "palette",                            # no-reference 폴백 (F8)
        "site-build",                         # built HTML 은 current/ 밖 (결정 4)
        "UNVERIFIED (engine unavailable)",    # D3 degrade
        "verify-runs/",                       # AC-1b
        "manifest",                           # D4 artifact-set
        "uvx",                                # 검증된 러너 형태 (결정 1)
    ):
        assert token in text, f"site.md: {token!r} missing"


def test_site_external_links_not_in_default_gate():
    # spec §7 ③ 동형: 외부 링크는 네트워크 의존 — 기본 게이트 제외, lychee 설치 시 선택
    text = (FORMATS_DIR / "site.md").read_text(encoding="utf-8")
    assert "lychee" in text and "optional" in text
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_format_cards.py -q`
Expected: FAIL — `FileNotFoundError: .../references/formats/site.md`

- [ ] **Step 3: 카드 작성** — `references/formats/site.md` 전문 (repo-docs.md의 레지스터·표 형식 승계):

````markdown
# site — MkDocs + Material documentation site (genre card)

> **What this is**: a *genre* card — the deliverable is a static documentation site whose
> **source** is an artifact-set (`outputs/<slug>/current/` = `mkdocs.yml` + `docs/` tree,
> plus `.omd/<slug>/manifest.json`; layout SSOT: `references/output-layout.md`). The build
> engine is borrowed (MkDocs + Material). Built HTML is a *derived* artifact — it lives in
> the work area (`.omd/<slug>/site-build/`), never inside `current/`.
> Documented alternative (R5+, no card): Docusaurus — Node/React ecosystem,
> `onBrokenLinks: 'throw'` as its machine gate.

## Engine

| Tool | Role | Version (measured) | Status |
|:---|:---|:---|:---|
| MkDocs (via `uvx --from mkdocs --with mkdocs-material mkdocs`) | build + `--strict` machine gate (gate ①) | — | UNVERIFIED (initial — the R3 measurement task stamps this machine) |
| mkdocs-material | theme; its `palette:` schema is this genre's no-reference fallback (F8) | — | UNVERIFIED |
| markdownlint-cli2 (via `npx`) | gate ② lint | v0.23.0 (markdownlint v0.41.0) | VERIFIED ✓ — 2026-07-13, markdownlint-cli2 v0.23.0 (R2 stamp, same runner) |
| lychee | external-link check (optional item) | — | UNVERIFIED — optional; network-dependent, excluded from the default gate |
| python3 stdlib | nav-completeness / placeholder / link fixture checks | measured at dogfooding | see `tests/test_site_dogfood.py` |

Runner note (measured 2026-07-13): the verified invocation is **ephemeral** —
`uvx --from mkdocs --with mkdocs-material mkdocs …` (uv-managed Python; a system
python3 too old for the toolchain is irrelevant to this path). No global install is
required or wanted; the runner form above IS the verified path.

Engine-missing rule (D3): a required engine that is not installed yields the verdict
`UNVERIFIED (engine unavailable)` for that item — state honestly what was not checked.
Never a silent PASS, never a FAIL on the environment. (Engine-drift demotion per
`references/formats/README.md` applies once versions are pinned.)

## Structure frame — Diátaxis quadrants (what doc-planner consumes)

Map the outline onto the four Diátaxis quadrants — the axes are *action vs cognition* ×
*acquisition vs application*:

| Quadrant | Serves | Form |
|:---|:---|:---|
| tutorials/ | learning by doing | a lesson — safe, guaranteed steps |
| how-to/ | a goal at work | a recipe — assumes competence |
| reference/ | information lookup | dry, complete, structured facts |
| explanation/ | understanding | discussion of why — context, design, trade-offs |

Rules doc-planner applies: one page serves ONE quadrant (mixing tutorial and reference on
a page is the classic failure); the nav's top level mirrors the quadrants; keep nav depth
≤ 2 (two-click reachability); every quadrant present or its absence justified in the
outline. Intake scope for this set genre: full site / a single quadrant / single-page
revision — the choice lands in the manifest `role` fields (D4).

## Standard mkdocs.yml skeleton (the validation block is NOT optional)

```yaml
site_name: <name>
theme:
  name: material
  palette:            # no-reference fallback (F8): pick primary/accent + scheme here,
    - scheme: default #   never from references/themes/ (office palettes)
      primary: indigo
      accent: indigo
validation:           # REQUIRED — without this block, `--strict` passes builds that
  nav:                #   silently drop pages (see Hard traps)
    omitted_files: warn
    not_found: warn
    absolute_links: warn
  links:
    not_found: warn
    anchors: warn
    unrecognized_links: warn
nav:
  - Home: index.md
  # top level mirrors the Diátaxis quadrants
```

## Hard traps

- **`--strict` alone does not catch nav-omitted pages** — `mkdocs build --strict` aborts
  on WARNING, but a `docs/**.md` file missing from `nav:` emits INFO by default, so a page
  can silently drop out of a green strict build. The `validation: nav: omitted_files: warn`
  line promotes it to WARNING = strict failure. [source: MkDocs user guide (configuration
  › validation) — accessed 2026-07-13 · candidate, the R3 measurement task promotes this
  with observed log lines]
- **Default output lands inside the source set** — `mkdocs build` writes `site/` next to
  `mkdocs.yml`, i.e. *inside* `outputs/<slug>/current/`, breaking the D4 invariant (the
  current entry is the source set, exactly one entry). Always build with
  `-d <workspace>/.omd/<slug>/site-build` — built HTML is derived evidence, the text
  edition of `renders/current/`. [source: omd D4 output-layout invariant, R3 plan 결정 4 —
  2026-07-13]
- **mkdocs-material config-key drift across majors** — Material renames/moves theme keys
  between major versions; a copied config from an old blog post fails on current Material.
  Pin behavior by measuring on this machine (Engine table), not by trusting snippets.
  [source: Material for MkDocs changelog/upgrade guide — accessed 2026-07-13 · candidate,
  promote with the measured Material version]
- **CJK search** — Material's built-in search tokenizes CJK poorly by default (`lang`/
  `separator` tuning or a plugin may be needed). Verify by inspecting the generated
  `search_index.json` for CJK tokens before promising search over Korean pages.
  [source: Material for MkDocs (search setup) — accessed 2026-07-13 · candidate, measure
  when a Korean page enters a pilot]

## Verify gate (docs-verify / doc-verifier run this — deterministic, exit-code first)

| # | Check | How |
|:--|:---|:---|
| ① | `mkdocs build --strict` exit 0 — **with the card's validation block present** | `uvx --from mkdocs --with mkdocs-material mkdocs build --strict -f <slug>/current/mkdocs.yml -d <workspace>/site-build`; missing engine → D3 degrade |
| ② | markdownlint-cli2 passes over `docs/` | `npx markdownlint-cli2 "outputs/<slug>/current/docs/**/*.md"`; missing engine → D3 degrade. Config discovery follows markdownlint-cli2 (a project-root config may relax rules) — the report names which config applied |
| ③ | Internal links & anchors resolve | covered by ①'s `validation.links` (not_found/anchors: warn + strict). External links: **optional** lychee item — network-dependent, excluded from the default gate |
| ④ | Built-HTML fresh-read | Read representative built pages from `.omd/<slug>/site-build/` — the render read-through, text edition. "The build ran" is not "the site is right" |
| ⑤ | nav completeness — every `docs/**.md` is in `nav:` | covered by ①'s `validation.nav.omitted_files: warn` + strict; the always-on stdlib double-check lives in the permanent guard (`tests/test_site_dogfood.py`), not in this gate |

Log capture (AC-1b): every borrowed-engine run's stdout+stderr →
`.omd/<slug>/verify-runs/<engine>-<timestamp>.log`, linked from the report.

## Version-snapshot policy

Artifact-set: a snapshot copies the **whole** `outputs/<slug>/current/` directory to
`.omd/<slug>/versions/v{NN}_{YYYY-MM-DD}_{summary}/` (LC-1; output-layout §3.3). Same
"only before a large edit" rule; `manifest.json` is rewritten atomically (ST-1) after any
member change. Built HTML (`site-build/`) is derived — never snapshotted.

## Placement (LC-2 — the user publishes, omd does not)

Deliverables stay in `outputs/<slug>/current/`. Publishing (GitHub Pages, `mkdocs
gh-deploy`, any hosting) is the user's lane — omd never deploys (§8). The manifest `role`
field records each file's site-relative place (`mkdocs.yml` → site root; pages → their
`docs/`-relative path).

## Form vs content (D7 boundary — this genre's examples)

Learnable FORM: nav-depth preference, palette scheme choice, quadrant balance, page-length
convention. Permanently forbidden CONTENT: page facts, API names, link targets, code
snippets' substance.

## Sources

- Diátaxis: https://diataxis.fr/
- MkDocs configuration & validation: https://www.mkdocs.org/user-guide/configuration/
- Material for MkDocs: https://squidfunk.github.io/mkdocs-material/
- markdownlint-cli2: https://github.com/DavidAnson/markdownlint-cli2
- lychee: https://github.com/lycheeverse/lychee
````

- [ ] **Step 4: green 확인**

Run: `python3 -m pytest tests/test_format_cards.py tests/ -q`
Expected: 전부 PASS (site 신규 2건 포함; `test_genre_cards_carry_required_sections`가 site.md의 필수 3섹션을 자동 커버)

- [ ] **Step 5: Commit** — `git add references/formats/site.md tests/test_format_cards.py && git commit -m "feat(omd): site genre card (UNVERIFIED initial) + card contract tests"`

---

### Task 2: site 루브릭 카드

**Files:**
- Create: `references/rubrics/site-rubric.md`
- Modify: `tests/test_genre_generalization.py`

**Interfaces:**
- Produces: `references/rubrics/site-rubric.md` — doc-inspector의 정성 렌즈 구성 SSOT (PS-3 동적 렌즈: site는 2렌즈). 기계 축은 site 카드 gate 소관임을 명시.

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_genre_generalization.py`의 "T7: repo-docs 루브릭" 섹션 아래에 추가:

```python
def test_site_rubric_exists_and_splits_axes():
    text = _ref("rubrics/site-rubric.md")
    assert "verify gate" in text          # 기계 축(빌드/링크)은 카드 게이트 소관 명시
    assert "Diátaxis" in text             # 정보 구조 축의 프레임 원출처
    assert "no self-approval" in text     # ppteval·repo-docs 와 동일한 분리 규칙
    for lens in ("Information architecture", "Prose quality"):
        assert lens in text, f"qualitative lens {lens!r} missing"
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_genre_generalization.py::test_site_rubric_exists_and_splits_axes -q`
Expected: FAIL — `FileNotFoundError: .../references/rubrics/site-rubric.md`

- [ ] **Step 3: 루브릭 작성** — `references/rubrics/site-rubric.md` 전문 (repo-docs-rubric.md 구조 승계):

````markdown
# Evaluation Rubric — site axes

> `doc-inspector` uses this card formatively (improvement points, ranked). `doc-verifier`
> does NOT read this card — the mechanical axes live in the site card's verify gate
> (`references/formats/site.md`). Dimension frame source: **Diátaxis** (diataxis.fr) —
> the quadrant model supplies the information-architecture axis.

## Axis split (who checks what)

| Axis | Nature | Checked by |
|:---|:---|:---|
| Build & link integrity (strict build, lint, internal links/anchors, nav completeness) | mechanical | site card verify gate ①②③⑤ — never an inspector lens |
| Information architecture | qualitative | inspector lens 1 |
| Prose quality | qualitative | inspector lens 2 |

## Lens 1 — Information architecture

- Diátaxis coverage: does each quadrant exist, and does each page serve exactly ONE
  quadrant? (a tutorial that drifts into reference is the classic failure)
- nav shape: top level mirrors the quadrants; depth ≤ 2; two-click reachability to any page.
- Entry flow: does index.md route each audience to the right quadrant in one hop?

## Lens 2 — Prose quality

- Style-guide adherence (pick one voice; consistent person/tense; sentence length sanity).
- Quadrant-appropriate register: tutorials speak in guaranteed steps, reference stays dry
  and complete, explanation argues trade-offs — register bleed is a finding.
- Vale is the optional mechanical assist here (not installed → note it, D3 spirit); the
  lens itself is judgment, not lint.

## Lens composition rule (PS-3)

The inspector runs exactly the qualitative axes above — for site that is **2 lenses**
(build/link integrity is already mechanical in the verify gate). Composition is per-genre
and fixed by the rubric card; the skill body never hardcodes a lens list.

## How the two reviewers split

Same rule as ppteval and repo-docs: the builder never inspects its own output as the
inspector, and the inspector never issues the verdict — **no self-approval** anywhere in
the lane. Verifier = mechanical gate + fresh evidence; inspector = these lenses.
````

- [ ] **Step 4: green 확인**

Run: `python3 -m pytest tests/test_genre_generalization.py tests/test_skill_contract.py -q`
Expected: PASS (skill-contract는 아직 site-rubric을 참조하는 본문이 없으므로 영향 없음)

- [ ] **Step 5: Commit** — `git add references/rubrics/site-rubric.md tests/test_genre_generalization.py && git commit -m "feat(omd): site rubric card — 2 qualitative lenses (PS-3 dynamic composition)"`

---

### Task 3: mkdocs 엔진 실측 검증 → 카드 VERIFIED 스탬프 + traps 실측 승격

**Files:**
- Modify: `references/formats/site.md` (Engine 표 스탬프 + Hard traps candidate→measured)
- Modify: `tests/test_format_cards.py` (스탬프 pin 테스트)
- 실측 샌드박스: `/tmp` 또는 `.omd/mkdocs-probe/` (비커밋)

**Interfaces:**
- Consumes: Task 1의 site.md.
- Produces: VERIFIED 스탬프 문자열(`VERIFIED ✓ — 2026-07-13, mkdocs <v> + mkdocs-material <v> (uvx)`) — Task 8 E2E가 이 검증 경로를 그대로 사용.

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_format_cards.py` 끝에 추가:

```python
def test_site_card_mkdocs_engine_stamped():
    """R3 실측 태스크의 완료 정의 — MkDocs 행이 이 머신에서 VERIFIED + 버전 핀."""
    text = (FORMATS_DIR / "site.md").read_text(encoding="utf-8")
    engine = _engine_section(text)
    mkdocs_rows = [l for l in engine.splitlines() if l.startswith("| MkDocs")]
    assert mkdocs_rows, "site.md: MkDocs engine row missing"
    assert "VERIFIED ✓" in mkdocs_rows[0], "MkDocs row not yet stamped"
    assert VERSION_RE.search(mkdocs_rows[0]), "MkDocs row pins no version"
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_format_cards.py::test_site_card_mkdocs_engine_stamped -q`
Expected: FAIL — "MkDocs row not yet stamped" (Task 1의 카드는 UNVERIFIED 출발)

- [ ] **Step 3: 실측 프로브 실행** — 임시 샌드박스에 결함을 의도적으로 심은 최소 사이트를 만들어 4가지를 측정:

```bash
P=$(mktemp -d); mkdir -p $P/docs/sub
cat > $P/mkdocs.yml <<'EOF'
site_name: probe
theme: {name: material}
nav:
  - Home: index.md
EOF
printf '# Home\n\n[broken](missing.md)\n\n[anchor](index.md#no-such)\n' > $P/docs/index.md
printf '# Orphan — nav 에 없음\n\n한국어 검색 토큰 측정용 문단.\n' > $P/docs/sub/orphan.md
# ⚠️ exit 캡처: `| tee ...; echo $?` 는 tee 의 exit 를 읽는다(ultracode 검증 blocking 실측) —
#    반드시 직접 리다이렉트 후 $? 를 변수로 잡을 것. strict 실패 빌드도 -d 에 HTML 을 남기므로
#    index.html 존재는 성공 증거가 아니다 — exit code 만이 게이트다.
# (1) validation 블록 없이 --strict: orphan(INFO)이 통과하는가, broken link 는 잡는가
uvx --from mkdocs --with mkdocs-material mkdocs build --strict -f $P/mkdocs.yml -d $P/out > $P/probe1.log 2>&1; ec1=$?
echo "probe1 exit=$ec1"; tail -5 $P/probe1.log
# (2) validation 블록 추가 후 재실행: omitted_files 가 WARNING→strict 실패로 승격되는가
cat >> $P/mkdocs.yml <<'EOF'
validation:
  nav: {omitted_files: warn, not_found: warn, absolute_links: warn}
  links: {not_found: warn, anchors: warn, unrecognized_links: warn}
EOF
uvx --from mkdocs --with mkdocs-material mkdocs build --strict -f $P/mkdocs.yml -d $P/out > $P/probe2.log 2>&1; ec2=$?
echo "probe2 exit=$ec2"; tail -5 $P/probe2.log
# (3) 엔진 버전 핀
uvx --from mkdocs --with mkdocs-material python -c "from importlib.metadata import version; print('mkdocs', version('mkdocs')); print('mkdocs-material', version('mkdocs-material'))"
# (4) CJK 검색: orphan 을 nav 에 넣고 green 빌드 후 search_index.json 에 한국어 토큰이 있는가
#     (mktemp -d 는 macOS 에서 $TMPDIR 아래에 생김 — /tmp/* glob 금지, $P 를 직접 쓸 것)
head -c 400 "$P/out/search/search_index.json" 2>/dev/null || echo "search index missing"
```
관찰을 기록: probe1(블록 없음)에서 orphan이 INFO로 통과하면 카드 trap 1 확증, broken link/anchor의 기본 레벨(warn이면 strict가 잡음), probe2에서 omitted_files가 WARNING으로 승격되어 exit≠0인지, 정확한 버전 2개, search_index의 CJK 토큰 형태.

- [ ] **Step 4: 카드 갱신** — 실측 결과로 site.md를 수정. **증거 표기는 카드 저작 계약(references/formats/README.md:15)의 브래킷 문법을 따를 것** (ultracode 검증 major 반영):
  - Engine 표 MkDocs 행: `— | UNVERIFIED (initial …)` → `1.6.1 + material <실측버전> | VERIFIED ✓ — 2026-07-13, mkdocs 1.6.1 + mkdocs-material <실측버전> (uvx)` (버전은 Step 3-(3) 실측값). mkdocs-material 행도 동일 스탬프.
  - Hard traps: 실측된 트랩은 `[VERIFIED ✓ — 2026-07-13, mkdocs <v> + mkdocs-material <v>]`, 표준 문서 인용에 머무는 트랩은 `[source: MkDocs user guide (validation) — accessed 2026-07-13]` 형식 — repo-docs.md 의 트랩들이 전부 이 브래킷 마커를 쓰는 것과 동일. **실측이 카드 서술과 다르면 카드를 실측에 맞춰 고치고** (예: broken link 기본 레벨이 이미 warn이라 validation 블록 없이도 strict가 잡는다면 그렇게 기록), gate ⑤ 관련 결정 5의 분기를 적용 — omitted_files 승격이 실측으로 확인되면 gate ⑤ 서술 유지, 아니면 gate ⑤를 stdlib 독립 체크로 승격 서술.
  - CJK 트랩은 실측 관찰(토큰 형태)을 적되, 파일럿이 영문이므로 `[source: …]` candidate 유지 가능.
  - **UNVERIFIED 출하 fallback (spec §7 리스크 ① — 하드 블로킹 금지)**: 실측이 이 태스크 범위에서 VERIFIED에 도달하지 못하면(엔진 다운로드 불가·예상 밖 함정 규모), 카드를 UNVERIFIED로 두고 `test_site_card_mkdocs_engine_stamped`에 `@pytest.mark.skip(reason="engine measurement deferred — spec §7 risk ①, card ships UNVERIFIED")`을 붙여 suite green을 유지한 채 진행한다. 이 경우 Task 8의 gate ①·② 실행은 D3 degrade(`UNVERIFIED (engine unavailable)`) 표명으로 대체되고 그 사실을 릴리스 노트에 기재.

- [ ] **Step 5: green 확인 + Commit**

Run: `python3 -m pytest tests/ -q`
Expected: 전부 PASS
`git add references/formats/site.md tests/test_format_cards.py && git commit -m "feat(omd): mkdocs engine measured on this machine — card stamped VERIFIED, traps promoted"`

---

### Task 4: route_emit — site 토큰 광고 해제 (R2 pin 테스트 반전)

**Files:**
- Modify: `hooks/route_emit.py`
- Modify: `tests/test_route_emit.py`

**Interfaces:**
- Consumes: Task 1의 site.md 실존 (카드 실존 규칙 — 카드 없는 포맷 광고는 H1 동형 결함).

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_route_emit.py`:

(a) `test_site_not_advertised_before_card_exists` (169-173행)를 **반전 교체**:
```python
def test_site_advertised_with_card():
    """R3: site 카드가 실재하므로 FORMAT 슬롯 광고 해제 (R2 pin 의 반전 — 광고는 카드 실존과 동기)."""
    card = Path(__file__).parent.parent / "references" / "formats" / "site.md"
    assert card.is_file(), "advertising requires the card to exist (H1-isomorphic rule)"
    out = context_of(run_hook({"prompt": "문서 사이트 만들어줘"}))
    stage_lines = [l for l in out.splitlines() if l.startswith("STAGE(docs)")]
    assert stage_lines and "site" in stage_lines[0]
```
(b) `test_context_lists_formats`의 튜플에 site 추가:
```python
    for fmt in ("pptx", "docx", "xlsx", "hwpx", "repo-docs", "site"):
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_route_emit.py -q`
Expected: 2건 FAIL (STAGE 줄·FORMAT 열거에 site 부재)

- [ ] **Step 3: 훅 수정** — `hooks/route_emit.py` CHECKPOINT 3곳:

24행:
```python
    "- 포맷: pptx / docx / xlsx / hwpx / repo-docs / site — references/formats/<format>.md 카드가 도구·함정·수식의 단일 진실.\n"
```
25-26행 (R3 이연 문구 제거, site 주석 병기):
```python
    "  (repo-docs = README·CHANGELOG 등 GitHub 저장소 문서 세트 / site = MkDocs+Material 문서 사이트 — "
    "둘 다 텍스트 장르, 산출은 artifact-set(outputs/<slug>/current/), 엔진은 마크다운+빌린 린터/빌더 체인.)\n"
```
43행:
```python
    "STAGE(docs) → <intake|standardize|plan|build|inspect|verify|revise|learn|docs-pilot> · <pptx|docx|xlsx|hwpx|repo-docs|site> · <한 줄 근거>\n"
```

- [ ] **Step 4: green 확인** — Run: `python3 -m pytest tests/test_route_emit.py tests/ -q` / Expected: 전부 PASS (route_emit 테스트 전반이 CHECKPOINT 본문을 assert하므로 전 파일 재검증 — F5)

- [ ] **Step 5: Commit** — `git commit -am "feat(omd): route checkpoint advertises site genre (card now exists — R2 pin inverted)"`

---

### Task 5: docs_verify_emit — mkdocs 신호 + verify-우선 매칭 (결정 2)

**Files:**
- Modify: `hooks/docs_verify_emit.py`
- Modify: `tests/test_verify_emit.py`

**Interfaces:**
- Produces: `is_verify_run(command)` — VERIFY_SIGNALS ∪ (`mkdocs`∧`--strict`). Task 8 E2E가 "strict 실행 후 센티널 clear"를 실증.

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_verify_emit.py` 끝에 추가 (기존 헬퍼 `run_hook`/`_with_slug` 재사용 — 파일 상단에 이미 존재):

```python
# ── R3: mkdocs 신호 (결정 2 — verify-우선 매칭) ───────────────────────

def test_mkdocs_plain_build_arms_sentinel(tmp_path):
    """plain `mkdocs build` = 산출 빌드 → site 리마인더 + 센티널 arm."""
    slug = _with_slug(tmp_path)
    out = run_hook(
        f"uvx --from mkdocs --with mkdocs-material mkdocs build "
        f"-f outputs/{slug}/current/mkdocs.yml -d .omd/{slug}/site-build",
        cwd=str(tmp_path),
    )
    assert (tmp_path / ".omd" / slug / ".verify-pending").is_file()
    assert out.strip() != ""
    assert "--strict" in out     # 오피스 shape-assertion 문구가 아니라 site 카드 gate 안내


def test_mkdocs_strict_build_clears_sentinel(tmp_path):
    """`mkdocs build --strict` = 카드 gate ① verify 실행 → arm 아니라 clear (결정 2)."""
    slug = _with_slug(tmp_path)
    sentinel = tmp_path / ".omd" / slug / ".verify-pending"
    sentinel.write_text("{}")
    out = run_hook(
        f"uvx --from mkdocs --with mkdocs-material mkdocs build --strict "
        f"-f outputs/{slug}/current/mkdocs.yml -d .omd/{slug}/site-build",
        cwd=str(tmp_path),
    )
    assert not sentinel.exists(), "strict run must clear, not re-arm"
    assert out.strip() == ""     # verify 실행에 빌드 리마인더를 얹지 않음


def test_office_verify_signals_regression(tmp_path):
    """verify-우선 매칭 도입 후에도 기존 3신호(clear)·빌드신호(arm) 동작 무변."""
    slug = _with_slug(tmp_path)
    sentinel = tmp_path / ".omd" / slug / ".verify-pending"
    sentinel.write_text("{}")
    run_hook(f"pdftoppm -png outputs/{slug}/x.pdf p", cwd=str(tmp_path))
    assert not sentinel.exists()
    run_hook("python3 -c 'from pptx import Presentation'", cwd=str(tmp_path))
    assert (tmp_path / ".omd" / ".verify-pending").is_file()  # slug 무언급 → 루트 센티널


def test_md_reminder_names_site_gate(tmp_path, monkeypatch):
    """md Edit|Write 리마인더가 site 카드 gate 도 안내 (repo-docs 하드코딩 해소)."""
    monkeypatch.setenv("OMD_REMINDER_COOLDOWN_SECONDS", "0")
    slug = _with_slug(tmp_path)
    out = run_md_hook(f"outputs/{slug}/current/docs/index.md", cwd=str(tmp_path))
    assert "site" in out and "mkdocs build --strict" in out
```
(⚠️ 헬퍼 시그니처 주의 — ultracode 검증 blocking 반영: 이 파일의 `run_hook`은 route_emit 테스트의 dict-payload 관례가 **아니라** `run_hook(command: str, tool_name="Bash", cwd=None, env=None)` (tests/test_verify_emit.py:28) — 커맨드 **문자열**을 첫 인자로, `cwd=`는 키워드로 넘긴다. dict를 넘기면 훅이 TypeError로 죽는다. `run_md_hook`/`_with_slug`도 기존 시그니처 그대로 재사용. throttle 간섭은 cooldown=0 으로 차단.)

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_verify_emit.py -q`
Expected: 신규 4건 FAIL (mkdocs 신호 부재·strict가 arm 되는 오동작·리마인더에 site 부재)

- [ ] **Step 3: 훅 수정** — `hooks/docs_verify_emit.py` 4곳:

(a) BUILD_SIGNALS (34-39행) 마지막에 추가:
```python
BUILD_SIGNALS = (
    "python-pptx", "python-docx", "from pptx", "import pptx",
    "from docx", "import docx", "Presentation(", "Document(",
    "openpyxl", "xlsxwriter", "Workbook(",
    "soffice --convert", "libreoffice --convert", "--convert-to",
    "mkdocs build",   # site genre — plain build arms; a --strict run is a verify (below)
)
```
(b) VERIFY_SIGNALS(53행) 아래에 판별 함수 신설:
```python
VERIFY_SIGNALS = ("pdftoppm", "unzip -t", "markdownlint")


def is_verify_run(command: str) -> bool:
    """Verify-stage runs clear the sentinel and are matched BEFORE build signals:
    `mkdocs build --strict` is the site card's gate ① (a verify), while a plain
    `mkdocs build` is a build. Flag order varies, so match the token pair."""
    if any(sig in command for sig in VERIFY_SIGNALS):
        return True
    return "mkdocs" in command and "--strict" in command
```
(c) main()의 흐름 재배열 — 기존:
```python
    # 문서 산출/변환 명령일 때만 리마인더. 그 외 Bash 는 침묵.
    if not is_doc_build(command):
        try:
            if any(sig in command for sig in VERIFY_SIGNALS):
                clear_sentinels(_omd_root(payload), command)
        except Exception:
            pass
        return 0
```
수정:
```python
    # verify 실행이 최우선 — strict 빌드가 BUILD_SIGNALS 에 걸려 재-arm 되는 것 방지 (결정 2).
    try:
        if command and is_verify_run(command):
            clear_sentinels(_omd_root(payload), command)
            return 0
    except Exception:
        pass  # fail-open

    # 문서 산출/변환 명령일 때만 리마인더. 그 외 Bash 는 침묵.
    if not is_doc_build(command):
        return 0
```
(d′) **build 리마인더도 장르 분기** (ultracode 검증 major 반영 — plain mkdocs build에 오피스 shape-assertion/zip-CRC 문구가 나가면 안 됨): `build_reminder()` 아래에 site 변형을 신설하고 main()의 리마인더 선택을 분기:
```python
def site_build_reminder() -> str:
    return (
        "[OMD document-integrity reminder] site 빌드 명령이 실행됨 (mkdocs).\n"
        "- 산출 빌드 후 docs-verify 로 카드 gate 를 실행할 것 — references/formats/site.md: "
        "mkdocs build --strict [validation 블록 필수] / markdownlint / 내부 링크·앵커 / "
        "built HTML fresh-read / nav 완결성.\n"
        "- built HTML 은 .omd/<slug>/site-build/ — outputs/<slug>/current/ 안에 site/ 를 만들지 말 것 (D4).\n"
        "- ⚠️ fresh 실행 증거 없이 done 선언 금지 — '빌드가 돌았다'는 검증이 아니다."
    )
```
main()의 `reminder = build_reminder()` 를 다음으로 교체:
```python
    reminder = site_build_reminder() if "mkdocs" in command else build_reminder()
```

(d) `md_reminder()` (150-159행) — repo-docs 한 줄을 두 장르 병기로:
```python
def md_reminder(slug: str) -> str:
    return (
        "[OMD document-integrity reminder] 텍스트 산출물(.md) 편집 감지 (slug: %s).\n"
        "- 편집을 마치면 docs-verify 로 카드가 정의한 **verify gate** 를 실행할 것 — "
        "repo-docs 는 references/formats/repo-docs.md 의 gate(필수 섹션·순서 / 내부 링크 / "
        "markdownlint / 코드블록 언어 태그 / ISO 날짜·버전 역순 / placeholder 스캔), "
        "site 는 references/formats/site.md 의 gate(mkdocs build --strict [validation 블록 필수] / "
        "markdownlint / 내부 링크·앵커 / built HTML fresh-read / nav 완결성).\n"
        "- ⚠️ fresh 실행 증거 없이 done 선언 금지 — '읽어보니 괜찮다'는 검증이 아니다.\n"
        "- 다중 파일 산출물의 계약은 outputs/<slug>/current/ + .omd/<slug>/manifest.json "
        "(references/output-layout.md)." % slug
    )
```

- [ ] **Step 4: green 확인** — Run: `python3 -m pytest tests/test_verify_emit.py tests/ -q` / Expected: 전부 PASS (기존 31건 무수정 green — verify-우선 매칭은 오피스 신호와 교집합 없음)

- [ ] **Step 5: Commit** — `git commit -am "feat(omd): mkdocs build/verify signals — verify-first matching so --strict clears instead of re-arming"`

---

### Task 6: 앞단 site 어휘 — docs-intake + doc-analyzer

**Files:**
- Modify: `skills/docs-intake/SKILL.md`
- Modify: `agents/doc-analyzer.md`
- Modify: `tests/test_genre_generalization.py`

**Interfaces:**
- Consumes: site.md의 intake 스코프 정의(full site / single quadrant / single-page).

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_genre_generalization.py` T8/T9 섹션에 추가:

```python
def test_intake_names_site_frame():
    body = _skill("docs-intake")
    assert "Diátaxis" in body                # site 의 장르 프레임
    assert "repo-docs, site" in body         # 세트 스코프 게이트가 site 도 커버


def test_analyzer_genre_frame_covers_site():
    body = _agent("doc-analyzer")
    assert "Diátaxis" in body
    assert "mkdocs.yml" in body              # site 입력 화이트리스트의 구체물
```

- [ ] **Step 2: 실패 확인** — Run: `python3 -m pytest tests/test_genre_generalization.py -q` / Expected: 신규 2건 FAIL

- [ ] **Step 3: 본문 수정**

(a) `skills/docs-intake/SKILL.md` 13행 Purpose — 기존:
```
... the card defines each genre's intake questions (office: tone preset (defense/conference/lecture); repo-docs: artifact-set scope + genre section preset) — through Socratic questions. ...
```
수정:
```
... the card defines each genre's intake questions (office: tone preset (defense/conference/lecture); repo-docs: artifact-set scope + genre section preset; site: Diátaxis scope + nav depth + palette) — through Socratic questions. ...
```
(b) 같은 파일 Gate(31-33행) — 기존:
```
For set genres (repo-docs), additionally lock the **artifact-set scope** before format·kind —
full set / README only / community-health only (the card's set-scope gate); the choice lands
in the D4 manifest `role` fields.
```
수정:
```
For set genres (repo-docs, site), additionally lock the **artifact-set scope** before format·kind —
repo-docs: full set / README only / community-health only; site: full site / a single Diátaxis
quadrant / single-page revision (each card's set-scope gate); the choice lands in the D4
manifest `role` fields.
```
(c) `agents/doc-analyzer.md` 12행 Role 끝부분 — 기존 `— office: tone preset; repo-docs: set scope + genre preset).` → `— office: tone preset; repo-docs: set scope + genre preset; site: Diátaxis quadrant scope + nav frame).`
(d) 같은 파일 23행 Success_Criteria — 기존 `(office: tone preset defense / conference / lecture; repo-docs: artifact-set scope + genre preset).` → `(office: tone preset defense / conference / lecture; repo-docs: artifact-set scope + genre preset; site: Diátaxis quadrant scope + nav frame).`
(e) 같은 파일 32행 Input boundary — 문장 끝에 추가:
```
For site the whitelist is the existing site source (mkdocs.yml, the docs/ tree, nav) plus the list above — the same never-the-whole-codebase rule.
```
(f) 같은 파일 81행 Output_Format 표의 Genre frame 행 — 기존 `| Genre frame | office: defense / conference / lecture · repo-docs: set scope + genre preset / … |` → `| Genre frame | office: defense / conference / lecture · repo-docs: set scope + genre preset · site: quadrant scope + nav frame / … |`

- [ ] **Step 4: green 확인** — Run: `python3 -m pytest tests/ -q` / Expected: 전부 PASS (기존 `test_intake_genre_frames_from_card`·`test_analyzer_has_input_boundary_and_genre_frame`의 보존 assert 포함)

- [ ] **Step 5: Commit** — `git commit -am "feat(omd): front-pipeline vocabulary covers site (intake scope gate + analyzer genre frame/whitelist)"`

---

### Task 7: docs-build — Format_Knowledge site 행 + Gate/Steps/Output 텍스트 장르 일반화

**Files:**
- Modify: `skills/docs-build/SKILL.md`
- Modify: `tests/test_genre_generalization.py`

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_genre_generalization.py` T11 섹션에 추가:

```python
def test_build_knowledge_table_lists_site():
    body = _skill("docs-build")
    assert "references/formats/site.md" in body
    assert "site-build" in body              # built HTML 은 current/ 밖 (결정 4)


def test_build_gate_generalized_beyond_png():
    body = _skill("docs-build")
    assert "fresh-read" in body              # 텍스트 장르 sanity 증거
    assert "PNG" in body                     # 오피스 증거는 보존
```

- [ ] **Step 2: 실패 확인** — Run: `python3 -m pytest tests/test_genre_generalization.py -q` / Expected: 신규 2건 FAIL

- [ ] **Step 3: 본문 수정** — `skills/docs-build/SKILL.md` 4곳:

(a) Format_Knowledge 표(38행 repo-docs 행 아래)에 추가:
```
- site → references/formats/site.md (MkDocs+Material static docs site genre — source artifact-set [mkdocs.yml + docs/ tree in outputs/<slug>/current/], built HTML is derived and goes to .omd/<slug>/site-build/ [never inside current/], machine gate = mkdocs build --strict with the card's validation block, Diátaxis structure frame, no PNG render — built-HTML fresh-read instead)
```
(b) 39행 no-reference fallback — 기존 `(repo-docs: genre section presets) (F8)` → `(repo-docs: genre section presets; site: the mkdocs-material palette schema) (F8)`
(c) Gate(28-30행) — 기존:
```
**Gate 2 — content confirmation.** After building, show the builder's sanity render (PNG) to the user to confirm
the content is correct. (Not a craftsmanship judgment — that is inspect/verify.)
```
수정:
```
**Gate 2 — content confirmation.** After building, show the builder's sanity evidence to the user —
office formats: the sanity render (PNG); text genres: a fresh-read of the produced files (site: the
built HTML) — to confirm the content is correct. (Not a craftsmanship judgment — that is inspect/verify.)
```
(d) Steps 2(45행) — 기존 `→ its own PNG sanity render (`.omd/<slug>/renders/current/`).` → `→ its own sanity evidence (office: PNG render into \`.omd/<slug>/renders/current/\`; text genres: fresh-read — site builds into \`.omd/<slug>/site-build/\`).`
(e) Output(50행) 첫 문장 — 기존 `The final version the user sees is the single `outputs/<slug>/current.<ext>`.` → `The final version the user sees is the single \`outputs/<slug>/current.<ext>\` (single-file formats) or the \`outputs/<slug>/current/\` artifact-set directory (text genres).`

- [ ] **Step 4: green 확인** — Run: `python3 -m pytest tests/ -q` / Expected: 전부 PASS (skill-contract가 site.md 실존을 확인 — Task 1에서 이미 생성됨)

- [ ] **Step 5: Commit** — `git commit -am "feat(omd): docs-build knows the site card; gate/steps/output generalized beyond PNG (PS-1 완결)"`

---

### Task 8: E2E 파일럿 — omd 자체 문서 사이트 (spec §5 R3 acceptance)

**Files:**
- Create: `tests/fixtures/omd-site/current/mkdocs.yml` + `tests/fixtures/omd-site/current/docs/` 6페이지 (통과본 스냅샷 — 결정 3)
- Modify: `.markdownlint-cli2.jsonc` (주석 한 줄 — "repo-docs card exception layer" → "text-genre exception layer (repo-docs + site)", Step 3 참고 항목)
- 작업 경로(비커밋): `outputs/omd-site/current/**`, `.omd/omd-site/{manifest.json,verify-runs/,site-build/}`

**Interfaces:**
- Consumes: Task 1~7 전부 (카드 gate·훅 신호·러너 경로).
- Produces: `tests/fixtures/omd-site/current/` — Task 9 영구 가드의 대상.

- [ ] **Step 1: 소스 세트 저작** — `outputs/omd-site/current/`에 Write 도구로 작성 (md Edit|Write 훅이 발화해 `.omd/omd-site/.verify-pending`이 arm 되는지가 부수 증거 — `.omd/omd-site/` 디렉토리를 먼저 만들 것). `mkdocs.yml` 전문:

```yaml
site_name: oh-my-docs
site_description: Document-work orchestration harness for Claude Code
theme:
  name: material
  palette:
    - scheme: default
      primary: indigo
      accent: indigo
validation:
  nav:
    omitted_files: warn
    not_found: warn
    absolute_links: warn
  links:
    not_found: warn
    anchors: warn
    unrecognized_links: warn
nav:
  - Home: index.md
  - Tutorials:
      - Getting started: tutorials/getting-started.md
  - How-to:
      - Produce and verify a document: how-to/produce-and-verify.md
  - Reference:
      - Skills and agents: reference/skills-and-agents.md
      - Format cards: reference/format-cards.md
  - Explanation:
      - Design principles: explanation/design-principles.md
```
docs/ 6페이지 — Diátaxis 4분면, 각 페이지는 **명시된 소스에서만** 내용을 취한다(날조 금지, AC-3 동형: README.md·plugin.json·references/formats/*.md·references/output-layout.md 화이트리스트). 페이지별 계약:
  - `index.md` — omd 한 줄 정의 + 4분면 안내(각 quadrant 로 1클릭 링크). 소스: README 상단.
  - `tutorials/getting-started.md` — 설치(plugin marketplace)→첫 파이프라인(intake→plan→build→verify) 따라하기. 소스: README Quick-start/Skills 절.
  - `how-to/produce-and-verify.md` — "브리프에서 검증된 산출물까지" 레시피(docs-pilot 경유, verify gate 확인법). 소스: README + repo-docs/site 카드 gate 표.
  - `reference/skills-and-agents.md` — 12 스킬·5 에이전트 표(이름+한 줄 역할). 소스: plugin.json skills 목록 + README Architecture 블록.
  - `reference/format-cards.md` — 6 카드(pptx/docx/xlsx/hwpx/repo-docs/site) 표: 엔진·게이트 요지. 소스: 각 카드 Engine/Verify gate 절. ⚠️ repo-docs gate ⑦을 요약할 때 **placeholder 토큰 문자열을 그대로 나열하지 말 것** — Task 9의 `test_no_placeholder_tokens`가 fixture 전 페이지를 스캔하므로 자기 가드에 걸린다(ultracode 검증 발견). "미완성 마커 스캔" 식으로 추상 서술하거나 카드명을 참조.
  - `explanation/design-principles.md` — 카드가 SSOT / inspect·verify 분리(no self-approval) / fail-open advisory / artifact-set 레이아웃의 왜. 소스: README + output-layout.md.
  각 페이지: 코드펜스는 언어 태그 필수, placeholder 토큰(PL-3) 금지, 내부 링크는 상대경로.

- [ ] **Step 2: manifest 생성** — stdlib 스니펫 (Bash로 실행):
```bash
python3 - <<'EOF'
import hashlib, json, os, tempfile
root = "outputs/omd-site/current"
paths = []
for dp, _, fns in os.walk(root):
    for fn in sorted(fns):
        p = os.path.join(dp, fn)
        rel = os.path.relpath(p, root)
        h = hashlib.sha256(open(p, "rb").read()).hexdigest()
        paths.append({"path": rel, "sha256": h, "role": rel})
m = {"slug": "omd-site", "format": "site", "paths": paths}
os.makedirs(".omd/omd-site", exist_ok=True)
fd, tmp = tempfile.mkstemp(dir=".omd/omd-site")
with os.fdopen(fd, "w") as f: json.dump(m, f, indent=2)
os.replace(tmp, ".omd/omd-site/manifest.json")   # ST-1
print("manifest:", len(paths), "files")
EOF
```

- [ ] **Step 3: 카드 gate 실행 (verify-runs 캡처)**:
```bash
# ⚠️ `| tee; echo $?` 금지 — tee 의 exit 를 읽어 strict 실패가 0 으로 위장된다(ultracode 실측).
#    strict 실패 빌드도 -d 에 index.html 을 남기므로 파일 존재도 성공 증거가 아니다 — exit code 가 게이트.
mkdir -p .omd/omd-site/verify-runs
TS=$(date +%Y%m%dT%H%M%S)
uvx --from mkdocs --with mkdocs-material mkdocs build --strict \
  -f outputs/omd-site/current/mkdocs.yml -d "$PWD/.omd/omd-site/site-build" \
  > ".omd/omd-site/verify-runs/mkdocs-strict-$TS.log" 2>&1; ec1=$?
echo "gate① exit=$ec1"; tail -3 ".omd/omd-site/verify-runs/mkdocs-strict-$TS.log"
npx --yes markdownlint-cli2 "outputs/omd-site/current/docs/**/*.md" \
  > ".omd/omd-site/verify-runs/markdownlint-$TS.log" 2>&1; ec2=$?
echo "gate② exit=$ec2"; tail -3 ".omd/omd-site/verify-runs/markdownlint-$TS.log"
test "$ec1" -eq 0 -a "$ec2" -eq 0 && echo "GATES GREEN" || echo "GATE FAILURE — fix sources, rerun"
test ! -e outputs/omd-site/current/site && echo "no site/ inside current (D4 ok)"
test ! -e .omd/omd-site/.verify-pending && echo "sentinel cleared by strict run (Task 5 E2E ok)"
```
Expected: `GATES GREEN` (ec1=0·ec2=0 — echo 로 확인한 실제 변수값), mkdocs 로그 tail에 "Aborted" 없음, current/ 안에 site/ 없음, 센티널 clear.
참고(ultracode 검증 major): repo 루트의 `.markdownlint-cli2.jsonc`(R2 산물, MD013 등 5룰 해제)가 config 탐색 규칙상 site docs에도 적용된다 — 이는 의도된 공유 예외층으로 **승인**하고, 그 파일의 주석을 "repo-docs card exception layer" → "text-genre exception layer (repo-docs + site)"로 갱신한다(이 태스크에서 함께 커밋). gate ②의 보증 수준이 그 5룰만큼 느슨하다는 사실은 카드 gate ② 행의 config-discovery 문구가 이미 정직하게 표명.
gate ④: `.omd/omd-site/site-build/index.html`과 `reference/skills-and-agents/index.html`을 Read로 fresh-read — nav·본문이 소스와 일치하는지 확인(불일치 발견 시 소스 수정 후 Step 2부터 재실행).

- [ ] **Step 4: 통과본 스냅샷 커밋 (결정 3)**:
```bash
mkdir -p tests/fixtures/omd-site   # 부모 디렉토리까지 — BSD cp 는 중간 경로를 만들지 않음 (ultracode 실측)
cp -R outputs/omd-site/current tests/fixtures/omd-site/current
diff -rq outputs/omd-site/current tests/fixtures/omd-site/current && echo "snapshot identical"
```

- [ ] **Step 5: green 확인 + Commit**

Run: `python3 -m pytest tests/ -q`
Expected: 전부 PASS
`git add tests/fixtures/omd-site .markdownlint-cli2.jsonc && git commit -m "feat(omd): E2E pilot — omd's own docs site passes the site card gate (fixture snapshot committed)"`

---

### Task 9: site dogfood 영구 가드 (stdlib-only)

**Files:**
- Create: `tests/test_site_dogfood.py`

**Interfaces:**
- Consumes: Task 8의 `tests/fixtures/omd-site/current/`.

- [ ] **Step 1: 가드 테스트 작성** — `tests/test_site_dogfood.py` 전문:

```python
"""Site genre permanent guard — the committed pilot fixture (tests/fixtures/omd-site/)
must keep conforming to the site card's stdlib-checkable gate items forever.
Borrowed-engine items (mkdocs build --strict, markdownlint-cli2) run at dogfood/release
time via uvx/npx, NOT in the always-on suite (same rule as test_repo_docs_dogfood)."""
import re
from pathlib import Path

ROOT = Path(__file__).parent.parent
FIX = ROOT / "tests" / "fixtures" / "omd-site" / "current"
DOCS = FIX / "docs"
# PL-3 prompt tokens — same list as the repo-docs gate ⑦ (bare link brackets are legit)
PLACEHOLDER_RE = re.compile(r"\[Insert |TODO|TBD|CHANGEME|your-\w+-here|lorem ipsum", re.IGNORECASE)
# ponytail: naive line parse — the fixture nav must stay in the simple "- Title: path.md"
# form; upgrade to a yaml parser only if the fixture ever needs anchors/aliases.
NAV_MD_RE = re.compile(r"^\s*-\s+[^:]+:\s+(\S+\.md)\s*$", re.MULTILINE)


def _yml() -> str:
    return (FIX / "mkdocs.yml").read_text(encoding="utf-8")


def test_fixture_exists_with_config_and_docs():
    assert (FIX / "mkdocs.yml").is_file()
    assert (DOCS / "index.md").is_file()


def test_mkdocs_yml_pins_strict_validation_block():
    """카드 규칙: validation 블록 없는 --strict 는 nav 누락을 조용히 통과시킨다."""
    yml = _yml()
    assert "validation:" in yml
    assert "omitted_files: warn" in yml
    assert "anchors: warn" in yml


def test_nav_lists_every_docs_page():
    """gate ⑤ 의 stdlib 이중 체크 — 엔진 없이도 fixture 회귀를 잡는다."""
    nav_pages = set(NAV_MD_RE.findall(_yml()))
    disk_pages = {str(p.relative_to(DOCS)) for p in DOCS.rglob("*.md")}
    assert nav_pages == disk_pages, f"nav vs docs/ drift: {nav_pages ^ disk_pages}"


def test_diataxis_quadrants_present():
    for quadrant in ("tutorials", "how-to", "reference", "explanation"):
        assert (DOCS / quadrant).is_dir(), f"quadrant {quadrant!r} missing"


def test_no_placeholder_tokens():
    for page in DOCS.rglob("*.md"):
        text = page.read_text(encoding="utf-8")
        assert not PLACEHOLDER_RE.search(text), f"{page.name}: placeholder token (PL-3)"


def test_internal_md_links_resolve():
    for page in DOCS.rglob("*.md"):
        text = page.read_text(encoding="utf-8")
        for target in re.findall(r"\]\(([^)#\s]+\.md)(?:#[^)]*)?\)", text):
            if target.startswith(("http://", "https://")):
                continue
            assert (page.parent / target).resolve().is_file(), f"{page.name} → {target} broken"


def test_code_fences_carry_language_tags():
    for page in DOCS.rglob("*.md"):
        lines = page.read_text(encoding="utf-8").splitlines()
        opener = True
        for ln in lines:
            if ln.strip().startswith("```"):
                if opener:
                    assert ln.strip() != "```", f"{page.name}: untagged code fence"
                opener = not opener
```

- [ ] **Step 2: green 확인** (가드는 fixture가 이미 통과본이므로 즉시 green이어야 — 하나라도 FAIL이면 fixture 결함이니 Task 8로 되돌아가 수리)

Run: `python3 -m pytest tests/test_site_dogfood.py tests/ -q`
Expected: 전부 PASS

- [ ] **Step 3: Commit** — `git add tests/test_site_dogfood.py && git commit -m "test(omd): site dogfood permanent guard — stdlib gate items pinned on the pilot fixture"`

---

### Task 10: R2 이월 코스메틱 소진 (결정 8)

**Files:**
- Modify: `.claude-plugin/plugin.json` (description)
- Modify: `README.md` (Formats/Status/Architecture)
- Modify: `agents/doc-planner.md` (Final_Checklist·Deliberate 트리거)
- Modify: `tests/test_genre_generalization.py`

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_genre_generalization.py` 끝에 추가:

```python
def test_plugin_description_names_text_genres():
    import json
    meta = json.loads((ROOT / ".claude-plugin" / "plugin.json").read_text(encoding="utf-8"))
    assert "repo-docs" in meta["description"] and "site" in meta["description"]


def test_planner_checklist_generalized_beyond_arc():
    body = _agent("doc-planner")
    assert "exactly one structure frame" in body   # arc 고정 어휘 해소 (R2 이월)
```

- [ ] **Step 2: 실패 확인** — Run: `python3 -m pytest tests/test_genre_generalization.py -q` / Expected: 신규 2건 FAIL

- [ ] **Step 3: 수정**

(a) `.claude-plugin/plugin.json` 4행 — 기존:
```json
  "description": "Multi-agent orchestration harness specialized for document work (pptx/docx/xlsx/hwpx)",
```
수정:
```json
  "description": "Multi-agent orchestration harness specialized for document work (office: pptx/docx/xlsx/hwpx · text genres: repo-docs, site)",
```
(b) `README.md` 60-63행 Formats 단락 — 기존 `and **repo-docs** (README/CHANGELOG/community-health set, text genre).` → `and the text genres **repo-docs** (README/CHANGELOG/community-health set) and **site** (MkDocs + Material static docs site).`
(c) `README.md` 65-69행 Status 단락 끝 — 기존 `Roadmap: R3 MkDocs site genre → R4 knowledge lifecycle.` 을 다음으로 교체:
```
R3 v0.3.0 adds the site genre — MkDocs + Material card with machine-measured engine
stamps, `mkdocs build --strict` as the deterministic gate, Diátaxis structure frames, and
omd's own docs site built as the E2E pilot (fixture-pinned dogfood guard). Roadmap: R4
knowledge lifecycle.
```
(d) `README.md` 81-82행 Architecture 인벤토리 — `formats/ (pptx, docx, xlsx, hwpx, pdf, repo-docs)` → `formats/ (pptx, docx, xlsx, hwpx, pdf, repo-docs, site)`; `rubrics/ (ppteval, repo-docs-rubric)` → `rubrics/ (ppteval, repo-docs-rubric, site-rubric)`. ⚠️ 실제 파일은 이 목록이 줄바꿈·들여쓰기로 감싸져 있다 — old_string은 plan의 한 줄 인용이 아니라 **파일을 Read한 실물**에서 복사할 것. README 3행의 한 줄 소개는 의도적으로 office-only로 유지(≤120자 게이트 리스크 회피) — PR 본문에 의도임을 명시.
(e) `agents/doc-planner.md` 115행 Final_Checklist — 기존:
```
    - Did I choose exactly one narrative arc *for the outline* and justify it against the tone preset? (`--consensus`: did the ≥2 Options in plan.md converge to exactly one Final arc?)
```
수정:
```
    - Did I choose exactly one structure frame *for the outline* (office: narrative arc vs the tone preset; repo-docs: section preset; site: quadrant map) and justify it? (`--consensus`: did the ≥2 Options in plan.md converge to exactly one Final frame?)
```
(f) `agents/doc-planner.md` 58행 Deliberate 트리거 — 기존 `defense / committee review / external official talk.` → `defense / committee review / external official talk or publication (an externally published docs site counts).`

- [ ] **Step 4: green 확인** — Run: `python3 -m pytest tests/ -q` / Expected: 전부 PASS (특히 `test_repo_docs_dogfood.py`의 README 게이트·`test_agent_contract.py`의 `## Narrative Arc` 헤딩 보존이 깨지지 않아야 — Output_Format 헤딩은 건드리지 않음)

- [ ] **Step 5: Commit** — `git commit -am "chore(omd): R2 carryover cosmetics — plugin/README name text genres, planner checklist frame-generalized"`

---

### Task 11: Release v0.3.0

**Files:**
- Modify: `.claude-plugin/plugin.json` (version)
- Modify: `CHANGELOG.md`

- [ ] **Step 1: 버전 bump** — plugin.json 3행 `"version": "0.2.0"` → `"version": "0.3.0"` (다른 필드 무변).

- [ ] **Step 2: CHANGELOG 엔트리** — `## [Unreleased]` 바로 아래에 삽입 (릴리스 메타는 blockquote — R2 plan 결정 9 승계, 6유형 밖 `###` 금지):

```markdown
## [0.3.0] - 2026-07-13

Site genre: MkDocs + Material static documentation sites join the harness as a card
(D1 — no new skill), with machine-measured engine stamps and omd's own docs site as
the E2E pilot.

### Added
- `references/formats/site.md` — site genre card: uvx-run MkDocs engine table (measured
  on this machine), Diátaxis structure frames, standard mkdocs.yml skeleton with a
  mandatory `validation:` block, 5-item deterministic verify gate, built-HTML placement
  rule (`.omd/<slug>/site-build/`, never inside `current/`).
- `references/rubrics/site-rubric.md` — 2 qualitative lenses (Information architecture /
  Prose quality); build & link integrity stays mechanical in the card gate (PS-3).
- E2E pilot: omd's own docs site built through the pipeline and pinned as
  `tests/fixtures/omd-site/` with a stdlib permanent guard (`tests/test_site_dogfood.py`).
- `mkdocs build` signals in `docs_verify_emit.py` with verify-first matching — a
  `--strict` run clears the verify sentinel instead of re-arming it.

### Changed
- Route checkpoint advertises `site` (R2 pin test inverted — advertising synced to card
  existence); format enumerations updated across route tests.
- Front pipeline (docs-intake, doc-analyzer) and docs-build carry the site genre frame;
  docs-build gate/steps/output generalized beyond PNG evidence (fresh-read for text genres).
- Carryover cosmetics: plugin description and README name the text genres; doc-planner
  checklist asks for "exactly one structure frame" instead of a narrative arc.

> **Verification**: python3 -m pytest tests/ -q — <N> passed (Task 11 Step 3 실측값 기입) ·
> site pilot gates measured green: `mkdocs build --strict` exit 0 + markdownlint-cli2 exit 0
> (logs under `.omd/omd-site/verify-runs/`, uncommitted by policy).

> **Notes**: MQ-2 (fenced-JSON verify output) re-rejected at R3 — no consumer emerged
> (docs-revise consumes the table; verify-runs/ carries machine evidence); user-ratified at
> merge. lychee remains optional/UNVERIFIED. CJK search trap stays a candidate (pilot is
> English). Engine stamps use ephemeral runners (uvx/npx) — no global installs.
```

- [ ] **Step 3: 전체 검증 + fake-completion 스캔**

Run: `python3 -m pytest tests/ -q` → Expected: 전부 PASS (수치를 CHANGELOG `<N>`에 기입 — 예상 ~130)
Run: `grep -n '<N>' CHANGELOG.md` → Expected: 0건 (기입 누락 가드 — CHANGELOG 게이트들은 `<N>`을 못 잡는다, ultracode 검증 발견)
Run: `grep -rnE "test\.skip|\.only\b|XXX-TODO" tests/ skills/ agents/ hooks/ references/ | grep -v fixtures` → Expected: 0건 (단 Task 3이 UNVERIFIED fallback으로 `@pytest.mark.skip`을 썼다면 그 1건은 사유 문자열과 함께 릴리스 노트에 기재된 의도된 예외)
Run: `python3 -m pytest tests/test_version_sync.py tests/test_repo_docs_dogfood.py tests/test_site_dogfood.py -q` → Expected: PASS

- [ ] **Step 4: Commit** — `git add .claude-plugin/plugin.json CHANGELOG.md && git commit -m "feat(omd): release v0.3.0 — site genre (MkDocs + Material), measured engine stamps, E2E pilot"`

---

## 이후 절차 (plan 밖 — spec §6, SDD 스킬이 수행)

whole-branch 리뷰(opus) → blocking 수리 → push + draft PR (본문에 사용자 비준 항목 명시: ① MQ-2 재기각(결정 6) ② E2E 파일럿의 fixture 스냅샷 커밋 — **신규 메커니즘**, 동결 스냅샷 vs 살아있는 dogfood 트레이드오프 포함(결정 3) ③ 이월 코스메틱 범위 + README 3행 office-only 유지(결정 8) ④ markdownlint 루트 config가 site docs에도 적용되는 text-genre 공유 예외층 승인(Task 8 참고 항목)) → 사용자 merge 게이트(`--no-ff`) → merge 후 v0.3.0 tag + marketplace update + **앱 재시작**(DT-4 캐시 함정).

## Self-review 기록 (writing-plans 체크)

- **Spec coverage**: §5 R3 4항목 전부 태스크 존재 — site 카드+루브릭(T1·T2), 엔진 실측 스탬프(T3), Diátaxis plan 지식(T1 카드 Structure frame 절 + T6 앞단 — doc-planner/docs-plan은 R2가 이미 site 명명, 무수정 확인됨), E2E 파일럿(T8·T9). route site 토큰(R2 비준 ④ 이행 = T4), BUILD_SIGNALS mkdocs(§4.4-2 R3 몫 = T5). spec §7 리스크 ①(실측 규모 초과 시 UNVERIFIED 출하 허용)은 T3 Step 4의 명시적 fallback(`@pytest.mark.skip` + 카드 UNVERIFIED 유지 + T8 D3 degrade 표명)으로 흡수 — 하드 블로킹 없음. ultracode 계획 검증(6 검증관, blocking 2계열·major 6·minor 6) 전 항목 반영 완료.
- **Build order**: 카드(T1)→실측(T3)이 라우팅 광고(T4)보다 먼저(카드 실존 규칙), 신호(T5)와 앞단(T6·T7)이 파일럿(T8)보다 먼저, 가드(T9)는 fixture 생성 후, 코스메틱(T10)→릴리스(T11) 마지막. G7 동형 논리(규칙 확립이 소비자보다 먼저) 준수.
- **Placeholder scan**: 코드 스텝 전부 실제 내용 포함. T8 페이지 본문은 산출물(콘텐츠 생성 태스크)이므로 페이지별 계약+소스 화이트리스트+기계 게이트로 정의 — R2 dogfooding(T13) 선례와 동일한 고도.
- **Type consistency**: 센티널 경로(`.omd/<slug>/.verify-pending`)·manifest 스키마(`{slug,format,paths[{path,sha256,role}]}`)·러너 문자열(`uvx --from mkdocs --with mkdocs-material mkdocs`)이 T1 카드·T5 훅·T8 파일럿·T9 가드에서 동일.
